---
import { LOCALES } from "@/i18n";

export function getStaticPaths() {
  return Object.keys(LOCALES).map((lang) => ({ params: { lang } }));
}

const lang = Astro.params.lang ?? "zh-cn";
return Astro.redirect(`/${lang}/rss.xml`, 301);
---
