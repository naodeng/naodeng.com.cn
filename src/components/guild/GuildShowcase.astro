---
/**
 * Guild 首页展示 — Apple 探索风格（与首页其它区块统一）
 */
import { getGuildConfig } from '../../config/guild.config';
import type { Lang } from '@/i18n';

interface Props {
  locale: Lang;
}

const { locale } = Astro.props;
const guildConfig = getGuildConfig(locale);

const visibleTypes = guildConfig.testTypes.filter(
  (t) => t.id === 'api-testing' || t.id === 'performance-testing'
);

const totalFrameworks = visibleTypes.reduce((n, t) => n + t.frameworks.length, 0);
const isZh = locale === 'zh-cn';
---

<section class="guild-showcase" aria-labelledby="guild-showcase-title">
  <div class="gs-header">
    <h2 id="guild-showcase-title" class="gs-title">
      {isZh ? '测试自动化指南' : 'Test Automation Guild'}
    </h2>
    <p class="gs-subtitle">
      {isZh
        ? '系统化的测试自动化学习路径，从入门到进阶'
        : 'Systematic test automation learning paths, from beginner to advanced'}
    </p>
    <p class="gs-stats">
      <span>{visibleTypes.length}{isZh ? ' 测试类型' : ' test types'}</span>
      <span aria-hidden="true">·</span>
      <span>{totalFrameworks}{isZh ? ' 框架' : ' frameworks'}</span>
    </p>
  </div>

  <div class="gs-sections">
    {visibleTypes.map((testType) => (
      <div class="gs-type-block" data-type={testType.id}>
        <div class="gs-type-header">
          <div class="gs-type-meta">
            <h3 class="gs-type-title">{testType.title}</h3>
            <p class="gs-type-desc">{testType.description}</p>
          </div>
          <a
            href={`/${locale}/guild/${testType.id}/`}
            class="gs-type-link"
            aria-label={isZh ? `查看${testType.title}全部内容` : `View all ${testType.title}`}
          >
            {isZh ? '查看全部' : 'View all'}
          </a>
        </div>

        <div class="gs-frameworks">
          {testType.frameworks.map((fw) => (
            <a
              href={`/${locale}/guild/${testType.id}/${fw.id}/`}
              class="gs-fw-card"
              title={fw.description}
            >
              <div class="gs-fw-card-top">
                {fw.icon ? (
                  <img
                    src={fw.icon}
                    alt=""
                    class="gs-fw-icon"
                    width="28"
                    height="28"
                    loading="lazy"
                    onerror="this.style.display='none'"
                  />
                ) : null}
                <span class="gs-fw-lang">{fw.language}</span>
              </div>
              <span class="gs-fw-name">{fw.name}</span>
              <span class="gs-fw-desc">{fw.description}</span>
              <span class="gs-fw-more">{isZh ? '了解更多 →' : 'Learn more →'}</span>
            </a>
          ))}
        </div>
      </div>
    ))}
  </div>

  <div class="gs-footer">
    <a href={`/${locale}/guild/`} class="gs-cta">
      {isZh ? '查看完整指南' : 'View Full Guide'}
    </a>
  </div>
</section>

<style>
  .guild-showcase {
    margin: 0 auto;
    max-width: 980px;
    padding: 0;
    background: transparent;
    border: none;
  }

  .gs-header {
    text-align: center;
    margin-bottom: 28px;
  }

  .gs-title {
    margin: 0;
    font-size: 28px;
    font-weight: 600;
    letter-spacing: -0.2px;
    line-height: 1.2;
    color: #1d1d1f;
  }

  .gs-subtitle {
    margin: 8px auto 0;
    max-width: 36em;
    font-size: 17px;
    line-height: 1.45;
    color: #7a7a7a;
  }

  .gs-stats {
    margin: 12px 0 0;
    font-size: 14px;
    color: #7a7a7a;
    display: flex;
    justify-content: center;
    gap: 8px;
  }

  .gs-sections {
    display: flex;
    flex-direction: column;
    gap: 24px;
    text-align: start;
  }

  .gs-type-block {
    background: #fff;
    border: 1px solid #e0e0e0;
    border-radius: 18px;
    padding: 24px;
  }

  .gs-type-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 16px;
    padding-bottom: 16px;
    border-bottom: 1px solid #f0f0f0;
  }

  .gs-type-title {
    margin: 0 0 6px;
    font-size: 17px;
    font-weight: 600;
    letter-spacing: -0.37px;
    color: #1d1d1f;
  }

  .gs-type-desc {
    margin: 0;
    font-size: 14px;
    line-height: 1.45;
    color: #7a7a7a;
  }

  .gs-type-link {
    flex-shrink: 0;
    color: var(--color-theme);
    font-size: 14px;
    text-decoration: none;
    min-height: 44px;
    display: inline-flex;
    align-items: center;
  }

  .gs-frameworks {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
  }

  .gs-fw-card {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 16px;
    border-radius: 18px;
    background: #fafafc;
    border: 1px solid #e0e0e0;
    text-decoration: none;
    color: inherit;
    box-shadow: none;
  }

  .gs-fw-card:active {
    transform: scale(0.98);
  }

  .gs-fw-card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
  }

  .gs-fw-icon {
    width: 28px;
    height: 28px;
    object-fit: contain;
    border-radius: 6px;
  }

  .gs-fw-lang {
    font-size: 12px;
    color: #7a7a7a;
  }

  .gs-fw-name {
    font-size: 17px;
    font-weight: 600;
    letter-spacing: -0.37px;
    color: #1d1d1f;
  }

  .gs-fw-desc {
    font-size: 14px;
    line-height: 1.45;
    color: #7a7a7a;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    flex: 1;
  }

  .gs-fw-more {
    margin-top: 4px;
    font-size: 14px;
    color: var(--color-theme);
  }

  .gs-footer {
    display: flex;
    justify-content: center;
    margin-top: 28px;
  }

  .gs-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 11px 22px;
    border-radius: 9999px;
    font-size: 17px;
    font-weight: 400;
    text-decoration: none;
    color: var(--color-theme);
    background: transparent;
    border: 1px solid var(--color-theme);
  }

  .gs-cta:active {
    transform: scale(0.95);
  }

  @media (width < 900px) {
    .gs-frameworks {
      grid-template-columns: 1fr;
    }

    .gs-type-header {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>
