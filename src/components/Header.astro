---
import LocaleSelect from "@/components/i18n/LocaleSelect.astro";
import { useTranslations, type Lang } from "@/i18n";
import { getRelativeLocaleUrl } from "astro:i18n";
import {
  SITE_TITLE,
  SITE_SLOGAN,
  NAV_HOME,
  NAV_ABOUT,
  NAV_PROJECTS,
  NAV_QA_WIKI,
  NAV_AI_WIKI,
  NAV_ENCYCLOPEDIA,
  NAV_GUILD,
  NAV_GUIDES,
  NAV_AI_TESTING,
  NAV_MORE,
  NAV_QA_PROMPTS,
  NAV_QA_SKILLS,
  NAV_SPONSOR,
  NAV_SEARCH,
  LEARNING_SITES,
} from "@/consts";

const t = useTranslations(Astro.currentLocale as Lang);
const locale = Astro.currentLocale as Lang;

const pathWithoutLang = Astro.url.pathname.replace(/^\/[a-zA-Z-]+/, "") || "/";

const isExactPath = (path: string) => {
  const localizedPath = getRelativeLocaleUrl(locale, path);
  return Astro.url.pathname === localizedPath || Astro.url.pathname === `${localizedPath}/`;
};

const usesZhCobrandLogos = locale === "zh-cn";

const isSectionPath = (path: string) => pathWithoutLang === path || pathWithoutLang.startsWith(`${path}/`);
const isWikiSection = () => isSectionPath("/wiki");
const isAiWikiSection = () => isSectionPath("/AIWiki");
const isGuildSection = () => isSectionPath("/guild");
const isPromptsSection = () => isSectionPath("/prompts");
const isQASkillsSection = () => isSectionPath("/qaskills");
const isProjectsSection = () => isSectionPath("/projects");
const isResourcesSection = () => isSectionPath("/resources");
const isSponsorSection = () => isSectionPath("/sponsor");
const siteTitle = t(SITE_TITLE);

type NavItem = {
  key: string;
  href: string;
  label: string;
  icon: string;
  active: boolean;
  external?: boolean;
};

type NavGroup = {
  key: string;
  label: string;
  active: boolean;
  items: NavItem[];
};

const navGroups: NavGroup[] = [
  {
    key: "encyclopedia",
    label: t(NAV_ENCYCLOPEDIA),
    active: isWikiSection() || isAiWikiSection(),
    items: [
      {
        key: "qa-wiki",
        href: locale === "zh-cn" ? getRelativeLocaleUrl(locale, "/wiki") : "https://ray.run/wiki",
        label: t(NAV_QA_WIKI),
        icon: "menu_book",
        active: isWikiSection(),
        external: locale !== "zh-cn",
      },
      {
        key: "ai-wiki",
        href: getRelativeLocaleUrl(locale, "/AIWiki"),
        label: t(NAV_AI_WIKI),
        icon: "psychology",
        active: isAiWikiSection(),
      },
    ],
  },
  {
    key: "guides",
    label: t(NAV_GUIDES),
    active: isGuildSection(),
    items: [
      {
        key: "guild",
        href: getRelativeLocaleUrl(locale, "/guild"),
        label: t(NAV_GUILD),
        icon: "route",
        active: isGuildSection(),
      },
    ],
  },
  {
    key: "ai-testing",
    label: t(NAV_AI_TESTING),
    active: isPromptsSection() || isQASkillsSection(),
    items: [
      {
        key: "qa-prompts",
        href: getRelativeLocaleUrl(locale, "/prompts"),
        label: t(NAV_QA_PROMPTS),
        icon: "tips_and_updates",
        active: isPromptsSection(),
      },
      {
        key: "qa-skills",
        href: getRelativeLocaleUrl(locale, "/qaskills"),
        label: t(NAV_QA_SKILLS),
        icon: "build",
        active: isQASkillsSection(),
      },
    ],
  },
  {
    key: "more",
    label: t(NAV_MORE),
    active: isProjectsSection() || isSponsorSection() || isResourcesSection(),
    items: [
      ...(locale === "zh-cn"
        ? LEARNING_SITES.map((site) => ({
            key: site.key,
            href: site.url,
            label: site.label["zh-cn"],
            icon: site.icon,
            active: false,
            external: true,
          }))
        : []),
      {
        key: "projects",
        href: getRelativeLocaleUrl(locale, "/projects"),
        label: t(NAV_PROJECTS),
        icon: "folder",
        active: isProjectsSection(),
      },
      {
        key: "resources",
        href: getRelativeLocaleUrl(locale, "/resources"),
        label: locale === "zh-cn" ? "资源导航" : "Resources",
        icon: "apps",
        active: isResourcesSection(),
      },
      {
        key: "sponsor",
        href: getRelativeLocaleUrl(locale, "/sponsor"),
        label: t(NAV_SPONSOR),
        icon: "favorite",
        active: isSponsorSection(),
      },
    ],
  },
];

const mobilePriorityItems = [
  {
    key: "blog",
    href: getRelativeLocaleUrl(locale, "/blog"),
    label: t({ en: "Blog", "zh-cn": "博客" }),
    icon: "article",
  },
  {
    key: "wiki",
    href: locale === "zh-cn" ? getRelativeLocaleUrl(locale, "/wiki") : getRelativeLocaleUrl(locale, "/AIWiki"),
    label: locale === "zh-cn" ? "Wiki" : "AI Wiki",
    icon: locale === "zh-cn" ? "menu_book" : "psychology",
  },
  {
    key: "prompts",
    href: getRelativeLocaleUrl(locale, "/prompts"),
    label: locale === "zh-cn" ? "提示词" : "Prompts",
    icon: "tips_and_updates",
  },
];
---

<header class="l-header" data-header>
  <div class:list={["header-shell l-content", { "header-shell--en": locale === "en" }]}>
    <div class:list={["site-brand", { "site-brand--en": locale === "en" }]}>
      <div class="site-brand-row">
        <p class="site-brand-heading">
          <a href={getRelativeLocaleUrl(locale, "/")} class="site-brand-link">
            {!usesZhCobrandLogos && <img src="/favicon.svg" alt="" class="site-logo" width="24" height="24" />}
            {locale === "zh-cn" ? (
              <span class="site-title-cobrand" aria-label={siteTitle}>
                <span class="site-title-segment">
                  {usesZhCobrandLogos && (
                    <img
                      src="/brand/software-testing-students-logo-final.png"
                      alt=""
                      class="site-title-logo site-title-logo--students"
                      width="24"
                      height="24"
                    />
                  )}
                  <span class="site-title-primary">软件测试同学</span>
                </span>
                <span class="site-title-cobrand-x" aria-hidden="true">X</span>
                <span class="site-title-segment site-title-segment--secondary">
                  {usesZhCobrandLogos && (
                    <img
                      src="/favicon.svg"
                      alt=""
                      class="site-title-logo site-title-logo--naodeng"
                      width="24"
                      height="24"
                    />
                  )}
                  <span class="site-title-secondary">naodeng</span>
                </span>
              </span>
            ) : (
              <span>{siteTitle}</span>
            )}
          </a>
        </p>
        <p class="site-slogan">{t(SITE_SLOGAN)}</p>
      </div>
    </div>

    <nav class="site-nav" id="site-primary-nav" data-site-nav>
      <ul>
        <li class="mobile-priority-nav" aria-label={locale === "zh-cn" ? "推荐入口" : "Featured entries"}>
          {mobilePriorityItems.map((item) => (
            <a href={item.href} data-nav-priority={item.key}>
              <span class="material-icons-sharp mobile-priority-nav__icon" aria-hidden="true">
                {item.icon}
              </span>
              <span>{item.label}</span>
            </a>
          ))}
        </li>
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/")}
            class={isExactPath("/") ? "active" : null}
            data-nav-item="home"
          >
            {t(NAV_HOME)}
          </a>
        </li>
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/blog")}
            class={isSectionPath("/blog") ? "active" : null}
            data-nav-item="blog"
          >
            {t({ en: "Blog", "zh-cn": "博客" })}
          </a>
        </li>
        {
          navGroups.map((group) =>
            group.items.length === 1 ? (
              <li class="nav-group nav-group--link" data-nav-group={group.key}>
                <a
                  href={group.items[0].href}
                  class={group.active ? "active" : null}
                  data-nav-item={group.items[0].key}
                  target={group.items[0].external ? "_blank" : undefined}
                  rel={group.items[0].external ? "noopener noreferrer" : undefined}
                >
                  {group.label}
                </a>
              </li>
            ) : (
              <li class="nav-group" data-nav-group={group.key}>
                <details data-nav-details>
                  <summary class:list={["nav-group-trigger", { active: group.active }]}>
                    <span>{group.label}</span>
                    <span class="material-icons-sharp nav-group-chevron" aria-hidden="true">expand_more</span>
                  </summary>
                  <ul class="nav-submenu">
                    {group.items.map((item) => (
                      <li>
                        <a
                          href={item.href}
                          class={item.active ? "active" : null}
                          data-nav-item={item.key}
                          target={item.external ? "_blank" : undefined}
                          rel={item.external ? "noopener noreferrer" : undefined}
                        >
                          <span class="nav-submenu-icon material-icons-sharp" aria-hidden="true">{item.icon}</span>
                          <span class="nav-submenu-text">{item.label}</span>
                        </a>
                      </li>
                    ))}
                  </ul>
                </details>
              </li>
            ),
          )
        }
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/about")}
            class={isExactPath("/about") ? "active" : null}
            data-nav-item="about"
          >
            {t(NAV_ABOUT)}
          </a>
        </li>
      </ul>
    </nav>

    <div class="header-utils">
      <div class="nav-tool nav-tool--bar">
        <LocaleSelect compact />
      </div>
      {usesZhCobrandLogos && (
        <div class="header-wechat-entry">
          <button
            type="button"
            class="btn-icon header-wechat-follow"
            aria-label="关注公众号"
            aria-haspopup="dialog"
            data-wechat-follow
          >
            <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M8.7 3.5C4.45 3.5 1 6.25 1 9.65c0 1.95 1.16 3.7 2.96 4.82l-.72 2.2 2.62-1.28c.9.27 1.85.42 2.84.42 4.25 0 7.7-2.75 7.7-6.16S12.95 3.5 8.7 3.5Zm-2.58 4.2a1.02 1.02 0 1 1 0-2.04 1.02 1.02 0 0 1 0 2.04Zm5.17 0a1.02 1.02 0 1 1 0-2.04 1.02 1.02 0 0 1 0 2.04Z" />
              <path d="M23 14.02c0-2.91-2.95-5.28-6.58-5.28-.36 0-.71.03-1.05.07.03.28.05.56.05.84 0 4.01-3.83 7.18-8.63 7.18h-.22c1.1 1.5 3.12 2.48 5.43 2.48.85 0 1.66-.13 2.4-.36l2.24 1.1-.62-1.88C20.16 17.34 23 15.23 23 14.02Zm-8.79-1.5a.87.87 0 1 1 0-1.74.87.87 0 0 1 0 1.74Zm4.42 0a.87.87 0 1 1 0-1.74.87.87 0 0 1 0 1.74Z" />
            </svg>
          </button>
          <div class="wechat-follow-coachmark" data-wechat-coachmark hidden>
            <button type="button" class="wechat-follow-coachmark__action" data-wechat-coachmark-open>
              快速关注微信公众号吧
            </button>
            <button
              type="button"
              class="wechat-follow-coachmark__close"
              aria-label="关闭关注提示"
              data-wechat-coachmark-close
            >×</button>
          </div>
        </div>
      )}
      <button type="button" class="btn-icon" aria-label={t(NAV_SEARCH)} data-search-open>
        <span class="material-icons-sharp" aria-hidden="true">search</span>
      </button>
      <button
        type="button"
        class="btn-icon theme-toggle"
        aria-label={locale === "zh-cn" ? "切换主题" : "Change theme"}
        title={locale === "zh-cn" ? "切换主题" : "Change theme"}
        data-theme-toggle
        data-label-light={locale === "zh-cn" ? "切换到浅色主题" : "Switch to light theme"}
        data-label-dark={locale === "zh-cn" ? "切换到深色主题" : "Switch to dark theme"}
      >
        <span class="material-icons-sharp theme-toggle__light" aria-hidden="true">light_mode</span>
        <span class="material-icons-sharp theme-toggle__dark" aria-hidden="true">dark_mode</span>
      </button>
      <button
        type="button"
        class="btn-icon nav-toggle"
        data-nav-toggle
        aria-controls="site-primary-nav"
        aria-expanded="false"
        aria-label={locale === "zh-cn" ? "打开导航菜单" : "Open navigation menu"}
      >
        <span class="material-icons-sharp nav-toggle__open" aria-hidden="true">menu</span>
        <span class="material-icons-sharp nav-toggle__close" aria-hidden="true">close</span>
      </button>
    </div>
  </div>
  <button
    type="button"
    class="nav-backdrop"
    data-nav-backdrop
    tabindex="-1"
    aria-hidden="true"
    aria-label={locale === "zh-cn" ? "关闭导航菜单" : "Close navigation menu"}
  ></button>
</header>

<script>
  const HOVER_CLOSE_MS = 120;
  const DESKTOP_MQ = "(min-width: 834px)";
  const HOVER_MQ = "(hover: hover) and (pointer: fine)";

  const isDesktopNav = () => window.matchMedia(DESKTOP_MQ).matches;
  const canHoverNav = () => window.matchMedia(HOVER_MQ).matches;

  const closeAllDetails = (except?: HTMLDetailsElement | null) => {
    document.querySelectorAll("[data-nav-details]").forEach((node) => {
      if (!(node instanceof HTMLDetailsElement)) return;
      if (except && node === except) return;
      node.open = false;
    });
  };

  const syncToggleA11y = (toggle: HTMLButtonElement, open: boolean) => {
    toggle.setAttribute("aria-expanded", open ? "true" : "false");
    const openLabel = toggle.getAttribute("data-label-open") || "Open navigation menu";
    const closeLabel = toggle.getAttribute("data-label-close") || "Close navigation menu";
    toggle.setAttribute("aria-label", open ? closeLabel : openLabel);
  };

  const setNavOpen = (open: boolean) => {
    const header = document.querySelector("[data-header]");
    const toggle = document.querySelector("[data-nav-toggle]");
    if (!(header instanceof HTMLElement) || !(toggle instanceof HTMLButtonElement)) return;

    header.toggleAttribute("data-nav-open", open);
    syncToggleA11y(toggle, open);
    document.documentElement.classList.toggle("nav-drawer-open", open);

    if (!open) {
      closeAllDetails();
      return;
    }

    if (!isDesktopNav()) {
      const firstLink = header.querySelector<HTMLElement>("[data-site-nav] a");
      firstLink?.focus({ preventScroll: true });
    }
  };

  const bindHoverMenus = (detail: HTMLDetailsElement) => {
    if (detail.dataset.hoverBound) return;
    detail.dataset.hoverBound = "1";

    let openTimer = 0;
    let closeTimer = 0;
    const clearTimers = () => {
      if (openTimer) {
        window.clearTimeout(openTimer);
        openTimer = 0;
      }
      if (closeTimer) {
        window.clearTimeout(closeTimer);
        closeTimer = 0;
      }
    };

    detail.addEventListener("pointerenter", () => {
      if (!isDesktopNav() || !canHoverNav()) return;
      clearTimers();
      // 短延迟，避免与点击打开打架（pointerenter 后立刻 click 会先 toggle）
      openTimer = window.setTimeout(() => {
        closeAllDetails(detail);
        detail.open = true;
      }, 80);
    });

    detail.addEventListener("pointerleave", () => {
      if (!isDesktopNav() || !canHoverNav()) return;
      clearTimers();
      closeTimer = window.setTimeout(() => {
        detail.open = false;
      }, HOVER_CLOSE_MS);
    });
  };

  const initHeaderNav = () => {
    const header = document.querySelector("[data-header]");
    const toggle = document.querySelector("[data-nav-toggle]");
    const backdrop = document.querySelector("[data-nav-backdrop]");
    const nav = document.querySelector("[data-site-nav]");
    const detailNodes = Array.from(document.querySelectorAll("[data-nav-details]"));

    if (toggle instanceof HTMLButtonElement && !toggle.dataset.bound) {
      toggle.dataset.bound = "1";
      const isZh = document.documentElement.lang?.toLowerCase().startsWith("zh");
      toggle.setAttribute("data-label-open", isZh ? "打开导航菜单" : "Open navigation menu");
      toggle.setAttribute("data-label-close", isZh ? "关闭导航菜单" : "Close navigation menu");
      toggle.addEventListener("click", (event) => {
        event.stopPropagation();
        const open = !(header instanceof HTMLElement && header.hasAttribute("data-nav-open"));
        setNavOpen(open);
      });
    }

    if (backdrop instanceof HTMLElement && !backdrop.dataset.bound) {
      backdrop.dataset.bound = "1";
      backdrop.addEventListener("click", () => setNavOpen(false));
    }

    detailNodes.forEach((detail) => {
      if (!(detail instanceof HTMLDetailsElement)) return;
      bindHoverMenus(detail);

      if (detail.dataset.toggleBound) return;
      detail.dataset.toggleBound = "1";
      detail.addEventListener("toggle", () => {
        if (!detail.open) return;
        closeAllDetails(detail);
      });
    });

    nav?.querySelectorAll("a").forEach((link) => {
      if (link.dataset.navCloseBound) return;
      link.dataset.navCloseBound = "1";
      link.addEventListener("click", () => {
        if (!isDesktopNav()) setNavOpen(false);
      });
    });

    if (!document.documentElement.dataset.appleHeaderDocBound) {
      document.documentElement.dataset.appleHeaderDocBound = "1";

      document.addEventListener("click", (event) => {
        const target = event.target;
        if (!(target instanceof Node)) return;

        const currentHeader = document.querySelector("[data-header]");
        const inHeader = currentHeader instanceof HTMLElement && currentHeader.contains(target);

        // 桌面：点空白关闭下拉
        if (isDesktopNav()) {
          const details = Array.from(document.querySelectorAll("[data-nav-details]"));
          if (!details.some((detail) => detail.contains(target))) {
            closeAllDetails();
          }
          return;
        }

        // 移动：点顶栏外关闭抽屉
        if (
          currentHeader instanceof HTMLElement &&
          currentHeader.hasAttribute("data-nav-open") &&
          !inHeader
        ) {
          setNavOpen(false);
        }
      });

      document.addEventListener("keydown", (event) => {
        if (event.key !== "Escape") return;
        closeAllDetails();
        setNavOpen(false);
        const currentToggle = document.querySelector("[data-nav-toggle]");
        if (currentToggle instanceof HTMLButtonElement && !isDesktopNav()) {
          currentToggle.focus();
        }
      });

      window.matchMedia(DESKTOP_MQ).addEventListener("change", (event) => {
        if (event.matches) setNavOpen(false);
        else closeAllDetails();
      });
    }
  };

  initHeaderNav();
  document.addEventListener("astro:after-swap", initHeaderNav);

  const initThemeToggle = () => {
    document.querySelectorAll("[data-theme-toggle]").forEach((node) => {
      if (!(node instanceof HTMLButtonElement) || node.dataset.bound) return;
      node.dataset.bound = "1";

      const syncLabel = () => {
        const current = document.documentElement.dataset.theme || "light";
        const label = (current === "dark" ? node.dataset.labelLight : node.dataset.labelDark) || "Change theme";
        node.setAttribute("aria-label", label);
        node.setAttribute("title", label);
      };

      node.addEventListener("click", () => {
        const current = document.documentElement.dataset.theme || "light";
        const next = current === "dark" ? "light" : "dark";
        (window as Window & { setThemePreference?: (preference: string) => void }).setThemePreference?.(next);
        syncLabel();
      });
      window.addEventListener("themechange", syncLabel);
      syncLabel();
    });
  };
  initThemeToggle();
  document.addEventListener("astro:after-swap", initThemeToggle);
</script>

<style>
  .l-header {
    position: sticky;
    top: 0;
    z-index: 100;
    /* 视觉样式见下方 Zenix skin 块（白玻璃顶栏） */
  }
  .l-header::after {
    display: none;
  }
  .header-shell {
    max-inline-size: 1400px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.35rem;
    padding-inline: 28px;
    margin-inline: auto;
  }
  .header-shell--en {
    flex-direction: row;
    align-items: center;
    gap: 1.35rem;
  }
  .site-brand {
    display: flex;
    justify-content: flex-start;
    min-width: 0;
    flex-shrink: 0;
  }
  .site-brand-row {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    max-width: none;
  }
  .site-slogan {
    display: none;
  }
  .site-brand-heading {
    margin: 0;
    font-size: var(--text-xl);
    font-family: var(--english-font);
    font-weight: 600;
    letter-spacing: -0.16px;
    line-height: 1;
  }
  .site-brand-link {
    display: inline-flex;
    align-items: center;
    gap: 0;
    text-decoration: none;
    white-space: nowrap;
    transition: opacity 0.18s ease;
  }
  .site-brand-link:hover {
    opacity: 0.8;
  }
  .site-title-cobrand {
    display: inline-flex;
    align-items: center;
    gap: 0.28em;
    white-space: nowrap;
    line-height: 1;
  }
  .site-title-segment {
    display: inline-flex;
    align-items: center;
    gap: 0.32em;
    min-width: 0;
  }
  .site-title-logo {
    flex: 0 0 auto;
    width: 24px;
    height: 24px;
    object-fit: contain;
    border-radius: 50%;
  }
  .site-title-cobrand-x {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1em;
    height: 1em;
    padding-inline: 0.12em;
    margin-inline: 0.04em 0.08em;
    font-size: 0.55em;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: color-mix(in srgb, white 10%, transparent);
    border: 1px solid color-mix(in srgb, white 14%, transparent);
    border-radius: 999px;
  }
  .site-brand-link .site-logo {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    margin-inline-end: 0.4em;
    object-fit: contain;
  }
  .site-nav {
    display: flex;
    justify-content: flex-end;
    min-width: 0;
    flex: 1 1 auto;
  }
  .site-nav > ul {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 0;
    margin: 0;
    padding: 0;
    list-style: none;
    border: none;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
    backdrop-filter: none;
    width: max-content;
    max-width: none;
  }
  .site-nav > ul > li {
    display: flex;
    align-items: center;
    flex-shrink: 0;
  }
  .site-nav > ul > li > a,
  .nav-group-trigger {
    display: inline-flex;
    align-items: center;
    gap: 0.2rem;
    min-height: 60px;
    padding: 0 0.95rem;
    border-radius: 0;
    white-space: nowrap;
    text-decoration: none;
    font-size: var(--text-sm);
    font-weight: 400;
    letter-spacing: -0.16px;
    opacity: 0.8;
    background: transparent;
    box-shadow: none;
    transition: opacity 0.18s ease, background-color 0.18s ease;
    position: relative;
  }
  .site-nav > ul > li > a:hover,
  .site-nav > ul > li > a:active,
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:hover,
  .nav-group-trigger:focus-visible {
    opacity: 1;
    background: transparent;
  }
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:focus-visible {
    outline: 2px solid var(--color-theme);
    outline-offset: -2px;
  }
  .site-nav > ul > li > a.active,
  .nav-group-trigger.active {
    opacity: 1;
    background: transparent;
    box-shadow: none;
  }
  .site-nav > ul > li > a.active::after,
  .nav-group-trigger.active::after {
    content: "";
    position: absolute;
    left: 0.95rem;
    right: 0.95rem;
    bottom: 10px;
    height: 2px;
    background: color-mix(in srgb, white 72%, transparent);
  }
  .nav-group {
    position: relative;
  }
  .nav-group details {
    position: relative;
  }
  .nav-group summary {
    display: inline-flex;
    align-items: center;
    gap: 0.1rem;
    list-style: none;
    cursor: pointer;
  }
  .nav-group summary::-webkit-details-marker {
    display: none;
  }
  .nav-group-chevron {
    font-size: var(--text-xl);
    opacity: 0.7;
    transition: transform 0.18s ease, opacity 0.18s ease;
  }
  .nav-group details[open] .nav-group-chevron,
  .nav-group summary:hover .nav-group-chevron {
    opacity: 1;
  }
  .nav-group details[open] .nav-group-chevron {
    transform: rotate(180deg);
  }
  .nav-submenu {
    position: absolute;
    top: calc(100% - 2px);
    left: 0;
    z-index: 120;
    display: grid;
    gap: 0.14rem;
    width: max-content;
    min-width: 16rem;
    max-width: calc(100vw - 2rem);
    margin: 0;
    padding: 0.55rem;
    list-style: none;
    border: 1px solid color-mix(in srgb, white 12%, transparent);
    border-radius: var(--radius-md);
    box-shadow: 0 12px 28px color-mix(in srgb, black 45%, transparent);
    transform-origin: top center;
    animation: nav-submenu-in 0.16s ease-out;
  }
  @keyframes nav-submenu-in {
    from {
      opacity: 0;
      transform: translateY(-4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .nav-submenu {
      animation: none;
    }
  }

  :global(html[data-theme="dark"]) .l-header { background: var(--color-glass-strong); border-color: var(--color-glass-border); box-shadow: var(--shadow-glass-sm); }
  :global(html[data-theme="dark"]) .site-nav > ul,
  :global(html[data-theme="dark"]) .nav-tool :global(.locale-trigger),
  :global(html[data-theme="dark"]) .btn-icon { background: var(--color-glass-medium); border-color: var(--color-glass-border); }
  :global(html[data-theme="dark"]) .nav-submenu,
  :global(html[data-theme="dark"]) .nav-tool :global(.locale-panel) { background: var(--color-glass-solid); border-color: var(--color-glass-border); }
  :global(html[data-theme="dark"]) .site-nav > ul > li > a:hover,
  :global(html[data-theme="dark"]) .site-nav > ul > li > a.active,
  :global(html[data-theme="dark"]) .nav-group-trigger:hover,
  :global(html[data-theme="dark"]) .nav-group-trigger.active { background: var(--color-theme-soft); }
  .nav-group[data-nav-group="more"] .nav-submenu {
    left: auto;
    right: 0;
  }
  .nav-submenu::before {
    content: "";
    position: absolute;
    top: -10px;
    left: 0;
    width: 100%;
    height: 12px;
    background: transparent;
  }
  .nav-submenu li {
    display: block;
  }
  .nav-submenu a {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    min-height: 3rem;
    width: auto;
    min-width: max-content;
    padding: 0.6rem 0.95rem;
    border-radius: var(--radius-sm);
    background: transparent;
    border: 1px solid transparent;
    box-sizing: border-box;
    text-decoration: none;
    opacity: 0.88;
    transition: background 0.15s ease, opacity 0.15s ease;
  }
  .nav-submenu a:hover,
  .nav-submenu a:focus-visible,
  .nav-submenu a.active {
    opacity: 1;
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .nav-submenu a.active::after {
    display: none;
  }
  .nav-submenu-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.5rem;
    flex: 0 0 1.5rem;
    font-size: var(--text-lg);
  }
  .nav-submenu-text {
    line-height: 1.25;
    font-size: var(--text-sm);
    font-weight: 400;
    letter-spacing: -0.16px;
    flex: 1 1 auto;
  }
  .nav-backdrop {
    display: none;
  }
  .nav-tool {
    display: flex;
    align-items: center;
    margin-inline-start: 0.1rem;
    padding-inline-start: 0.35rem;
    border-inline-start: 1px solid color-mix(in srgb, white 16%, transparent);
  }
  .header-utils {
    display: flex;
    align-items: center;
    gap: 0.1rem;
    flex-shrink: 0;
    margin-inline-start: 0.25rem;
  }
  .header-utils .nav-tool--bar {
    margin-inline-start: 0;
    padding-inline-start: 0;
    border-inline-start: none;
  }
  .nav-toggle {
    display: none;
  }
  .nav-toggle__close {
    display: none;
  }
  .site-nav > ul > li.mobile-priority-nav {
    display: none;
  }
  .l-header[data-nav-open] .nav-toggle__open {
    display: none;
  }
  .l-header[data-nav-open] .nav-toggle__close {
    display: inline-flex;
  }
  .nav-tool :global(.locale-trigger) {
    height: 44px;
    min-height: 44px;
    min-width: 60px;
    padding-inline: 8px;
    font-size: var(--text-sm);
    font-weight: 400;
    border: none;
    border-radius: var(--radius-sm);
  }
  .nav-tool :global(.locale-trigger:hover),
  .nav-tool :global(.locale-trigger:focus-visible) {
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .nav-tool :global(.icon-lang),
  .nav-tool :global(.icon-expand) {
    opacity: 0.85;
    font-size: var(--text-lg);
  }
  .nav-tool :global(.locale-panel) {
    border: 1px solid color-mix(in srgb, white 12%, transparent);
    box-shadow: 0 12px 28px color-mix(in srgb, black 45%, transparent);
  }
  .nav-tool :global(.locale-panel a) {
    font-size: var(--text-sm);
    font-weight: 400;
  }
  .nav-tool :global(.locale-panel a:hover) {
    background: color-mix(in srgb, white 10%, transparent);
  }
  .btn-icon {
    display: inline-flex;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: center;
    height: 44px;
    min-width: 44px;
    padding-inline: 0.55rem;
    white-space: nowrap;
    font: inherit;
    font-size: var(--text-sm);
    font-weight: 400;
    background: transparent;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: background 0.18s ease, opacity 0.18s ease;
  }
  .btn-icon:hover,
  .btn-icon:focus-visible {
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .btn-icon:focus-visible {
    outline: 2px solid var(--color-theme);
    outline-offset: 1px;
  }
  .btn-icon .material-icons-sharp {
    font-size: var(--text-lg);
    color: currentColor;
    opacity: 0.95;
    flex-shrink: 0;
  }
  .header-wechat-follow {
    color: #07c160;
  }
  .header-wechat-entry {
    position: relative;
    display: inline-flex;
    flex: 0 0 auto;
  }
  .header-wechat-follow svg {
    width: 20px;
    height: 20px;
  }
  .wechat-follow-coachmark {
    position: absolute;
    z-index: 30;
    inset-block-start: calc(100% + 12px);
    inset-inline-start: 50%;
    display: flex;
    align-items: center;
    width: max-content;
    max-width: min(240px, calc(100vw - 32px));
    padding: 0.38rem 0.42rem 0.38rem 0.8rem;
    color: white;
    border: 1px solid color-mix(in srgb, #07c160 58%, white);
    border-radius: 999px;
    background: color-mix(in srgb, #07c160 88%, #082f24);
    box-shadow: 0 12px 30px rgba(7, 193, 96, 0.24);
    transform: translateX(-50%);
  }
  .wechat-follow-coachmark[hidden] {
    display: none;
  }
  .wechat-follow-coachmark::before {
    content: "";
    position: absolute;
    inset-block-start: -6px;
    inset-inline-start: 50%;
    width: 10px;
    height: 10px;
    background: inherit;
    border-block-start: 1px solid color-mix(in srgb, #07c160 58%, white);
    border-inline-start: 1px solid color-mix(in srgb, #07c160 58%, white);
    transform: translateX(-50%) rotate(45deg);
  }
  .wechat-follow-coachmark__action,
  .wechat-follow-coachmark__close {
    position: relative;
    z-index: 1;
    padding: 0;
    color: inherit;
    border: 0;
    background: transparent;
    cursor: pointer;
  }
  .wechat-follow-coachmark__action {
    font-size: 0.8rem;
    font-weight: 700;
    white-space: nowrap;
  }
  .wechat-follow-coachmark__close {
    display: grid;
    place-items: center;
    width: 26px;
    height: 26px;
    margin-inline-start: 0.35rem;
    font-size: 1rem;
    border-radius: 50%;
  }
  .wechat-follow-coachmark__close:hover,
  .wechat-follow-coachmark__close:focus-visible {
    background: rgba(255, 255, 255, 0.16);
  }
  .theme-toggle__dark { display: none; }
  :global(html[data-theme="dark"]) .theme-toggle__light { display: none; }
  :global(html[data-theme="dark"]) .theme-toggle__dark { display: inline-block; }

  @media (width < 834px) {
    .l-header {
      height: auto;
      min-height: 60px;
    }
    .header-shell {
      height: auto;
      min-height: 60px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      gap: 0.35rem;
      padding-block: 0;
      padding-inline: 1rem;
      position: relative;
      z-index: 2;
    }
    .site-brand,
    .site-brand-row {
      justify-content: flex-start;
      min-width: 0;
    }
    .site-brand {
      min-height: 60px;
      align-items: center;
      overflow: hidden;
    }
    .site-brand .site-brand-heading {
      min-width: 0;
      max-width: 100%;
      font-size: 1.05rem;
    }
    .site-brand-link {
      max-width: 100%;
      min-width: 0;
    }
    .site-title-cobrand {
      min-width: 0;
    }
    /* Keep brand readable; hide cobrand suffix that collided with utils */
    .site-title-cobrand-x,
    .site-title-segment--secondary {
      display: none;
    }
    .site-title-logo {
      width: 22px;
      height: 22px;
    }
    .header-utils {
      grid-column: 2;
      grid-row: 1;
      position: relative;
      z-index: 3;
      gap: 0;
    }
    .nav-tool :global(.locale-trigger) {
      min-width: 44px;
      padding-inline: 6px;
    }
    .nav-toggle {
      display: inline-flex;
    }
    .site-nav {
      display: block;
      grid-column: 1 / -1;
      grid-row: 2;
      width: 100%;
      max-height: 0;
      padding-block: 0;
      border-top: 0 solid transparent;
      overflow: hidden;
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      transition:
        max-height 0.22s ease,
        opacity 0.18s ease,
        padding-block 0.18s ease,
        border-color 0.18s ease,
        visibility 0s linear 0.22s;
    }
    .l-header[data-nav-open] .site-nav {
      max-height: min(70vh, 32rem);
      padding-block: 0.35rem 0.85rem;
      border-top: 1px solid color-mix(in srgb, white 12%, transparent);
      overflow: auto;
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transition:
        max-height 0.28s ease,
        opacity 0.18s ease,
        padding-block 0.18s ease,
        border-color 0.18s ease,
        visibility 0s;
      -webkit-overflow-scrolling: touch;
    }
    .nav-backdrop {
      display: block;
      position: fixed;
      inset: 60px 0 0;
      z-index: 1;
      border: 0;
      padding: 0;
      margin: 0;
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      cursor: pointer;
      transition: opacity 0.2s ease, visibility 0s linear 0.2s;
    }
    .l-header[data-nav-open] .nav-backdrop {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transition: opacity 0.2s ease, visibility 0s;
    }
    :global(html.nav-drawer-open) {
      overflow: hidden;
    }
    .site-nav > ul {
      flex-direction: column;
      flex-wrap: nowrap;
      align-items: stretch;
      justify-content: flex-start;
      gap: 0;
      width: 100%;
      max-width: 100%;
    }
    .site-nav > ul > li {
      width: 100%;
    }
    .site-nav > ul > li.mobile-priority-nav {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 0.45rem;
      padding: 0.65rem 0 0.75rem;
      border-bottom: 1px solid color-mix(in srgb, white 12%, transparent);
    }
    .mobile-priority-nav a {
      display: grid;
      justify-items: center;
      align-content: center;
      gap: 0.25rem;
      min-height: 74px;
      padding: 0.6rem 0.35rem;
      border-radius: var(--radius-sm);
      text-align: center;
      text-decoration: none;
      background: color-mix(in srgb, white 8%, transparent);
    }
    .mobile-priority-nav a:hover,
    .mobile-priority-nav a:focus-visible {
      background: color-mix(in srgb, white 14%, transparent);
    }
    .mobile-priority-nav__icon {
      font-size: var(--text-lg);
    }
    .site-nav > ul > li > a,
    .nav-group-trigger {
      min-height: 60px;
      width: 100%;
      justify-content: flex-start;
      padding-inline: 1rem;
    }
    .site-nav > ul > li > a.active::after,
    .nav-group-trigger.active::after {
      left: 1rem;
      right: auto;
      width: 1.7rem;
      bottom: 14px;
    }
    .nav-group details {
      width: 100%;
    }
    .nav-submenu {
      position: static;
      top: auto;
      left: auto;
      right: auto;
      z-index: auto;
      margin-top: 0.15rem;
      min-width: 0;
      width: 100%;
      max-width: none;
      box-shadow: none;
      border: none;
      background: color-mix(in srgb, white 6%, transparent);
      animation: none;
    }
    .nav-group[data-nav-group="more"] .nav-submenu,
    .nav-group[data-nav-group="ai-testing"] .nav-submenu {
      left: auto;
      right: auto;
    }
    .nav-submenu::before {
      display: none;
    }
  }

  @media (width < 834px) and (prefers-reduced-motion: reduce) {
    .site-nav,
    .l-header[data-nav-open] .site-nav,
    .nav-backdrop,
    .l-header[data-nav-open] .nav-backdrop {
      transition: none;
    }
  }

  /* A compact editorial navigation skin. Keep behavior and labels intact. */
  .l-header {
    height: 64px;
    color: var(--color-main);
    background: color-mix(in srgb, var(--color-base) 94%, transparent);
    border-block-end: 1px solid var(--color-border);
    box-shadow: none;
    backdrop-filter: blur(12px);
  }

  .header-shell {
    height: 64px;
    max-inline-size: 1440px;
    padding-inline: clamp(1rem, 3vw, 2rem);
  }

  .site-brand-heading {
    font-family: var(--font-heading);
    font-size: var(--text-xl);
    font-weight: 800;
  }

  .site-brand-link,
  .site-brand-link:hover {
    color: var(--color-main);
  }

  .site-title-cobrand {
    gap: 0.34em;
  }

  .site-title-primary,
  .site-title-secondary,
  .site-brand-link > span:not(.site-title-cobrand) {
    color: var(--color-theme);
  }

  .site-title-cobrand-x {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 10%, white);
    border-color: color-mix(in srgb, var(--color-theme) 22%, transparent);
  }

  .site-nav > ul {
    gap: 0.1rem;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
    backdrop-filter: none;
  }

  .site-nav > ul > li > a,
  .nav-group-trigger {
    min-height: 36px;
    padding: 0 0.65rem;
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text-secondary);
    opacity: 1;
  }

  .site-nav > ul > li > a:hover,
  .site-nav > ul > li > a:active,
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:hover,
  .nav-group-trigger:focus-visible,
  .site-nav > ul > li > a.active,
  .nav-group-trigger.active {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 8%, transparent);
  }

  .site-nav > ul > li > a.active::after,
  .nav-group-trigger.active::after {
    display: none;
  }

  .nav-group-chevron {
    color: currentColor;
  }

  .nav-submenu,
  .nav-tool :global(.locale-panel) {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-surface-elevated);
    box-shadow: var(--shadow-md);
    backdrop-filter: none;
  }

  .nav-submenu a,
  .nav-submenu-text,
  .nav-tool :global(.locale-panel a) {
    color: var(--color-main);
  }

  .nav-submenu-icon {
    color: var(--color-theme);
  }

  .nav-submenu a:hover,
  .nav-submenu a:focus-visible,
  .nav-tool :global(.locale-panel a:hover) {
    color: var(--color-main);
    background: color-mix(in srgb, var(--color-theme) 12%, var(--color-surface));
    border-color: color-mix(in srgb, var(--color-theme) 28%, var(--color-border));
  }

  .nav-submenu a.active {
    color: var(--color-main);
    background: color-mix(in srgb, var(--color-theme) 18%, var(--color-surface));
    border-color: color-mix(in srgb, var(--color-theme) 38%, var(--color-border));
  }

  .nav-submenu a.active .nav-submenu-icon {
    color: var(--color-theme);
  }

  .nav-submenu a:hover .nav-submenu-text,
  .nav-submenu a:focus-visible .nav-submenu-text,
  .nav-submenu a.active .nav-submenu-text {
    color: var(--color-main);
  }

  .header-utils {
    gap: 0.15rem;
  }

  .nav-toggle {
    display: none;
  }

  .nav-tool :global(.locale-trigger),
  .btn-icon {
    color: var(--color-text-secondary);
    background: transparent;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
  }

  .nav-tool :global(.locale-trigger:hover),
  .nav-tool :global(.locale-trigger:focus-visible),
  .btn-icon:hover,
  .btn-icon:focus-visible {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 8%, transparent);
    border-color: color-mix(in srgb, var(--color-theme) 18%, transparent);
  }

  .nav-tool :global(.icon-lang),
  .nav-tool :global(.icon-expand) {
    color: currentColor;
    opacity: 1;
  }

  @media (width < 834px) {
    .l-header,
    .header-shell {
      min-height: 60px;
      height: auto;
    }

    .site-brand {
      min-height: 60px;
    }

    .site-brand .site-brand-heading {
      font-size: 0.95rem;
    }

    .site-title-logo {
      width: 20px;
      height: 20px;
    }

    .header-utils {
      gap: 0.15rem;
    }

    .nav-tool :global(.locale-trigger),
    .btn-icon {
      width: 40px;
      min-width: 40px;
      height: 40px;
      padding-inline: 0;
    }

    .nav-toggle {
      display: inline-flex;
    }

    .l-header[data-nav-open] .site-nav {
      border-top-color: rgba(148, 163, 184, 0.22);
    }

    .site-nav > ul {
      border-radius: var(--radius-md);
      align-items: stretch;
      background: var(--color-surface-elevated);
    }

    .mobile-priority-nav {
      border-bottom-color: rgba(148, 163, 184, 0.22);
    }
    .mobile-priority-nav a {
      color: var(--color-text-secondary);
      background: color-mix(in srgb, var(--color-theme) 6%, white);
    }
    .mobile-priority-nav a:hover,
    .mobile-priority-nav a:focus-visible {
      color: var(--color-theme);
      background: color-mix(in srgb, var(--color-theme) 10%, white);
    }
    .mobile-priority-nav__icon {
      color: var(--color-theme);
    }

    .site-nav > ul > li > a,
    .nav-group-trigger {
      min-height: 52px;
    }

    .nav-submenu {
      background: color-mix(in srgb, var(--color-theme) 5%, white);
      border: 1px solid rgba(148, 163, 184, 0.18);
    }

    .nav-backdrop {
      inset: 60px 0 0;
      background: rgba(15, 23, 42, 0.28);
      backdrop-filter: blur(4px);
    }
  }
</style>
