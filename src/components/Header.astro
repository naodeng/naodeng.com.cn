---
import LocaleSelect from "@/components/i18n/LocaleSelect.astro";
import { useTranslations, type Lang } from "@/i18n";
import { getRelativeLocaleUrl } from "astro:i18n";
import {
  SITE_TITLE,
  SITE_SLOGAN,
  NAV_HOME,
  NAV_ABOUT,
  NAV_PROJECTS,
  NAV_QA_WIKI,
  NAV_AI_WIKI,
  NAV_ENCYCLOPEDIA,
  NAV_GUILD,
  NAV_GUIDES,
  NAV_AI_TESTING,
  NAV_MORE,
  NAV_QA_PROMPTS,
  NAV_QA_SKILLS,
  NAV_SPONSOR,
  NAV_SEARCH,
  LEARNING_SITES,
} from "@/consts";

const t = useTranslations(Astro.currentLocale as Lang);
const locale = Astro.currentLocale as Lang;

const pathWithoutLang = Astro.url.pathname.replace(/^\/[a-zA-Z-]+/, "") || "/";

const isExactPath = (path: string) => {
  const localizedPath = getRelativeLocaleUrl(locale, path);
  return Astro.url.pathname === localizedPath || Astro.url.pathname === `${localizedPath}/`;
};

const isSectionPath = (path: string) => pathWithoutLang === path || pathWithoutLang.startsWith(`${path}/`);
const isWikiSection = () => isSectionPath("/wiki");
const isAiWikiSection = () => isSectionPath("/AIWiki");
const isGuildSection = () => isSectionPath("/guild");
const isPromptsSection = () => isSectionPath("/prompts");
const isQASkillsSection = () => isSectionPath("/qaskills");
const isProjectsSection = () => isSectionPath("/projects");
const isSponsorSection = () => isSectionPath("/sponsor");
const siteTitle = t(SITE_TITLE);

type NavItem = {
  key: string;
  href: string;
  label: string;
  icon: string;
  active: boolean;
  external?: boolean;
};

type NavGroup = {
  key: string;
  label: string;
  active: boolean;
  items: NavItem[];
};

const navGroups: NavGroup[] = [
  {
    key: "encyclopedia",
    label: t(NAV_ENCYCLOPEDIA),
    active: isWikiSection() || isAiWikiSection(),
    items: [
      {
        key: "qa-wiki",
        href: locale === "zh-cn" ? getRelativeLocaleUrl(locale, "/wiki") : "https://ray.run/wiki",
        label: t(NAV_QA_WIKI),
        icon: "menu_book",
        active: isWikiSection(),
        external: locale !== "zh-cn",
      },
      {
        key: "ai-wiki",
        href: getRelativeLocaleUrl(locale, "/AIWiki"),
        label: t(NAV_AI_WIKI),
        icon: "psychology",
        active: isAiWikiSection(),
      },
    ],
  },
  {
    key: "guides",
    label: t(NAV_GUIDES),
    active: isGuildSection(),
    items: [
      {
        key: "guild",
        href: getRelativeLocaleUrl(locale, "/guild"),
        label: t(NAV_GUILD),
        icon: "route",
        active: isGuildSection(),
      },
    ],
  },
  {
    key: "ai-testing",
    label: t(NAV_AI_TESTING),
    active: isPromptsSection() || isQASkillsSection(),
    items: [
      {
        key: "qa-prompts",
        href: getRelativeLocaleUrl(locale, "/prompts"),
        label: t(NAV_QA_PROMPTS),
        icon: "tips_and_updates",
        active: isPromptsSection(),
      },
      {
        key: "qa-skills",
        href: getRelativeLocaleUrl(locale, "/qaskills"),
        label: t(NAV_QA_SKILLS),
        icon: "build",
        active: isQASkillsSection(),
      },
    ],
  },
  {
    key: "more",
    label: t(NAV_MORE),
    active: isProjectsSection() || isSponsorSection(),
    items: [
      ...(locale === "zh-cn"
        ? LEARNING_SITES.map((site) => ({
            key: site.key,
            href: site.url,
            label: site.label["zh-cn"],
            icon: site.icon,
            active: false,
            external: true,
          }))
        : []),
      {
        key: "projects",
        href: getRelativeLocaleUrl(locale, "/projects"),
        label: t(NAV_PROJECTS),
        icon: "folder",
        active: isProjectsSection(),
      },
      {
        key: "sponsor",
        href: getRelativeLocaleUrl(locale, "/sponsor"),
        label: t(NAV_SPONSOR),
        icon: "favorite",
        active: isSponsorSection(),
      },
    ],
  },
];

const mobilePriorityItems = [
  {
    key: "blog",
    href: getRelativeLocaleUrl(locale, "/blog"),
    label: t({ en: "Blog", "zh-cn": "博客" }),
    icon: "article",
  },
  {
    key: "wiki",
    href: locale === "zh-cn" ? getRelativeLocaleUrl(locale, "/wiki") : getRelativeLocaleUrl(locale, "/AIWiki"),
    label: locale === "zh-cn" ? "Wiki" : "AI Wiki",
    icon: locale === "zh-cn" ? "menu_book" : "psychology",
  },
  {
    key: "prompts",
    href: getRelativeLocaleUrl(locale, "/prompts"),
    label: locale === "zh-cn" ? "提示词" : "Prompts",
    icon: "tips_and_updates",
  },
];
---

<header class="l-header" data-header>
  <div class:list={["header-shell l-content", { "header-shell--en": locale === "en" }]}>
    <div class:list={["site-brand", { "site-brand--en": locale === "en" }]}>
      <div class="site-brand-row">
        <h1>
          <a href={getRelativeLocaleUrl(locale, "/")} class="site-brand-link">
            <img src="/favicon.svg" alt="" class="site-logo" width="24" height="24" />
            {locale === "zh-cn" ? (
              <span class="site-title-cobrand" aria-label={siteTitle}>
                <span class="site-title-primary">软件测试同学</span>
                <span class="site-title-cobrand-x" aria-hidden="true">X</span>
                <span class="site-title-secondary">naodeng</span>
              </span>
            ) : (
              <span>{siteTitle}</span>
            )}
          </a>
        </h1>
        <p class="site-slogan">{t(SITE_SLOGAN)}</p>
      </div>
    </div>

    <nav class="site-nav" id="site-primary-nav" data-site-nav>
      <ul>
        <li class="mobile-priority-nav" aria-label={locale === "zh-cn" ? "推荐入口" : "Featured entries"}>
          {mobilePriorityItems.map((item) => (
            <a href={item.href} data-nav-priority={item.key}>
              <span class="material-icons-sharp mobile-priority-nav__icon" aria-hidden="true">
                {item.icon}
              </span>
              <span>{item.label}</span>
            </a>
          ))}
        </li>
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/")}
            class={isExactPath("/") ? "active" : null}
            data-nav-item="home"
          >
            {t(NAV_HOME)}
          </a>
        </li>
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/blog")}
            class={isSectionPath("/blog") ? "active" : null}
            data-nav-item="blog"
          >
            {t({ en: "Blog", "zh-cn": "博客" })}
          </a>
        </li>
        {
          navGroups.map((group) =>
            group.items.length === 1 ? (
              <li class="nav-group nav-group--link" data-nav-group={group.key}>
                <a
                  href={group.items[0].href}
                  class={group.active ? "active" : null}
                  data-nav-item={group.items[0].key}
                  target={group.items[0].external ? "_blank" : undefined}
                  rel={group.items[0].external ? "noopener noreferrer" : undefined}
                >
                  {group.label}
                </a>
              </li>
            ) : (
              <li class="nav-group" data-nav-group={group.key}>
                <details data-nav-details>
                  <summary class:list={["nav-group-trigger", { active: group.active }]}>
                    <span>{group.label}</span>
                    <span class="material-icons-sharp nav-group-chevron" aria-hidden="true">expand_more</span>
                  </summary>
                  <ul class="nav-submenu">
                    {group.items.map((item) => (
                      <li>
                        <a
                          href={item.href}
                          class={item.active ? "active" : null}
                          data-nav-item={item.key}
                          target={item.external ? "_blank" : undefined}
                          rel={item.external ? "noopener noreferrer" : undefined}
                        >
                          <span class="nav-submenu-icon material-icons-sharp" aria-hidden="true">{item.icon}</span>
                          <span class="nav-submenu-text">{item.label}</span>
                        </a>
                      </li>
                    ))}
                  </ul>
                </details>
              </li>
            ),
          )
        }
        <li>
          <a
            href={getRelativeLocaleUrl(locale, "/about")}
            class={isExactPath("/about") ? "active" : null}
            data-nav-item="about"
          >
            {t(NAV_ABOUT)}
          </a>
        </li>
      </ul>
    </nav>

    <div class="header-utils">
      <div class="nav-tool nav-tool--bar">
        <LocaleSelect compact />
      </div>
      <button type="button" class="btn-icon" aria-label={t(NAV_SEARCH)} data-search-open>
        <span class="material-icons-sharp" aria-hidden="true">search</span>
      </button>
      <button
        type="button"
        class="btn-icon nav-toggle"
        data-nav-toggle
        aria-controls="site-primary-nav"
        aria-expanded="false"
        aria-label={locale === "zh-cn" ? "打开导航菜单" : "Open navigation menu"}
      >
        <span class="material-icons-sharp nav-toggle__open" aria-hidden="true">menu</span>
        <span class="material-icons-sharp nav-toggle__close" aria-hidden="true">close</span>
      </button>
    </div>
  </div>
  <button
    type="button"
    class="nav-backdrop"
    data-nav-backdrop
    tabindex="-1"
    aria-hidden="true"
    aria-label={locale === "zh-cn" ? "关闭导航菜单" : "Close navigation menu"}
  ></button>
</header>

<script>
  const HOVER_CLOSE_MS = 120;
  const DESKTOP_MQ = "(min-width: 834px)";
  const HOVER_MQ = "(hover: hover) and (pointer: fine)";

  const isDesktopNav = () => window.matchMedia(DESKTOP_MQ).matches;
  const canHoverNav = () => window.matchMedia(HOVER_MQ).matches;

  const closeAllDetails = (except?: HTMLDetailsElement | null) => {
    document.querySelectorAll("[data-nav-details]").forEach((node) => {
      if (!(node instanceof HTMLDetailsElement)) return;
      if (except && node === except) return;
      node.open = false;
    });
  };

  const syncToggleA11y = (toggle: HTMLButtonElement, open: boolean) => {
    toggle.setAttribute("aria-expanded", open ? "true" : "false");
    const openLabel = toggle.getAttribute("data-label-open") || "Open navigation menu";
    const closeLabel = toggle.getAttribute("data-label-close") || "Close navigation menu";
    toggle.setAttribute("aria-label", open ? closeLabel : openLabel);
  };

  const setNavOpen = (open: boolean) => {
    const header = document.querySelector("[data-header]");
    const toggle = document.querySelector("[data-nav-toggle]");
    if (!(header instanceof HTMLElement) || !(toggle instanceof HTMLButtonElement)) return;

    header.toggleAttribute("data-nav-open", open);
    syncToggleA11y(toggle, open);
    document.documentElement.classList.toggle("nav-drawer-open", open);

    if (!open) {
      closeAllDetails();
      return;
    }

    if (!isDesktopNav()) {
      const firstLink = header.querySelector<HTMLElement>("[data-site-nav] a");
      firstLink?.focus({ preventScroll: true });
    }
  };

  const bindHoverMenus = (detail: HTMLDetailsElement) => {
    if (detail.dataset.hoverBound) return;
    detail.dataset.hoverBound = "1";

    let openTimer = 0;
    let closeTimer = 0;
    const clearTimers = () => {
      if (openTimer) {
        window.clearTimeout(openTimer);
        openTimer = 0;
      }
      if (closeTimer) {
        window.clearTimeout(closeTimer);
        closeTimer = 0;
      }
    };

    detail.addEventListener("pointerenter", () => {
      if (!isDesktopNav() || !canHoverNav()) return;
      clearTimers();
      // 短延迟，避免与点击打开打架（pointerenter 后立刻 click 会先 toggle）
      openTimer = window.setTimeout(() => {
        closeAllDetails(detail);
        detail.open = true;
      }, 80);
    });

    detail.addEventListener("pointerleave", () => {
      if (!isDesktopNav() || !canHoverNav()) return;
      clearTimers();
      closeTimer = window.setTimeout(() => {
        detail.open = false;
      }, HOVER_CLOSE_MS);
    });
  };

  const initHeaderNav = () => {
    const header = document.querySelector("[data-header]");
    const toggle = document.querySelector("[data-nav-toggle]");
    const backdrop = document.querySelector("[data-nav-backdrop]");
    const nav = document.querySelector("[data-site-nav]");
    const detailNodes = Array.from(document.querySelectorAll("[data-nav-details]"));

    if (toggle instanceof HTMLButtonElement && !toggle.dataset.bound) {
      toggle.dataset.bound = "1";
      const isZh = document.documentElement.lang?.toLowerCase().startsWith("zh");
      toggle.setAttribute("data-label-open", isZh ? "打开导航菜单" : "Open navigation menu");
      toggle.setAttribute("data-label-close", isZh ? "关闭导航菜单" : "Close navigation menu");
      toggle.addEventListener("click", (event) => {
        event.stopPropagation();
        const open = !(header instanceof HTMLElement && header.hasAttribute("data-nav-open"));
        setNavOpen(open);
      });
    }

    if (backdrop instanceof HTMLElement && !backdrop.dataset.bound) {
      backdrop.dataset.bound = "1";
      backdrop.addEventListener("click", () => setNavOpen(false));
    }

    detailNodes.forEach((detail) => {
      if (!(detail instanceof HTMLDetailsElement)) return;
      bindHoverMenus(detail);

      if (detail.dataset.toggleBound) return;
      detail.dataset.toggleBound = "1";
      detail.addEventListener("toggle", () => {
        if (!detail.open) return;
        closeAllDetails(detail);
      });
    });

    nav?.querySelectorAll("a").forEach((link) => {
      if (link.dataset.navCloseBound) return;
      link.dataset.navCloseBound = "1";
      link.addEventListener("click", () => {
        if (!isDesktopNav()) setNavOpen(false);
      });
    });

    if (!document.documentElement.dataset.appleHeaderDocBound) {
      document.documentElement.dataset.appleHeaderDocBound = "1";

      document.addEventListener("click", (event) => {
        const target = event.target;
        if (!(target instanceof Node)) return;

        const currentHeader = document.querySelector("[data-header]");
        const inHeader = currentHeader instanceof HTMLElement && currentHeader.contains(target);

        // 桌面：点空白关闭下拉
        if (isDesktopNav()) {
          const details = Array.from(document.querySelectorAll("[data-nav-details]"));
          if (!details.some((detail) => detail.contains(target))) {
            closeAllDetails();
          }
          return;
        }

        // 移动：点顶栏外关闭抽屉
        if (
          currentHeader instanceof HTMLElement &&
          currentHeader.hasAttribute("data-nav-open") &&
          !inHeader
        ) {
          setNavOpen(false);
        }
      });

      document.addEventListener("keydown", (event) => {
        if (event.key !== "Escape") return;
        closeAllDetails();
        setNavOpen(false);
        const currentToggle = document.querySelector("[data-nav-toggle]");
        if (currentToggle instanceof HTMLButtonElement && !isDesktopNav()) {
          currentToggle.focus();
        }
      });

      window.matchMedia(DESKTOP_MQ).addEventListener("change", (event) => {
        if (event.matches) setNavOpen(false);
        else closeAllDetails();
      });
    }
  };

  initHeaderNav();
  document.addEventListener("astro:after-swap", initHeaderNav);
</script>

<style>
  .l-header {
    position: sticky;
    top: 0;
    z-index: 100;
    /* 视觉样式见下方 Zenix skin 块（白玻璃顶栏） */
  }
  .l-header::after {
    display: none;
  }
  .header-shell {
    max-inline-size: 1400px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.35rem;
    padding-inline: 28px;
    margin-inline: auto;
  }
  .header-shell--en {
    flex-direction: row;
    align-items: center;
    gap: 1.35rem;
  }
  .site-brand {
    display: flex;
    justify-content: flex-start;
    min-width: 0;
    flex-shrink: 0;
  }
  .site-brand-row {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    max-width: none;
  }
  .site-slogan {
    display: none;
  }
  h1 {
    margin: 0;
    font-size: var(--text-xl);
    font-family: var(--english-font);
    font-weight: 600;
    letter-spacing: -0.16px;
    line-height: 1;
  }
  .site-brand-link {
    display: inline-flex;
    align-items: center;
    gap: 0;
    text-decoration: none;
    white-space: nowrap;
    transition: opacity 0.18s ease;
  }
  .site-brand-link:hover {
    opacity: 0.8;
  }
  .site-title-cobrand {
    display: inline-flex;
    align-items: center;
    gap: 0.28em;
    white-space: nowrap;
    line-height: 1;
  }
  .site-title-cobrand-x {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1em;
    height: 1em;
    padding-inline: 0.12em;
    margin-inline: 0.04em 0.08em;
    font-size: 0.55em;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: color-mix(in srgb, white 10%, transparent);
    border: 1px solid color-mix(in srgb, white 14%, transparent);
    border-radius: 999px;
  }
  .site-brand-link .site-logo {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    margin-inline-end: 0.4em;
    object-fit: contain;
  }
  .site-nav {
    display: flex;
    justify-content: flex-end;
    min-width: 0;
    flex: 1 1 auto;
  }
  .site-nav > ul {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 0;
    margin: 0;
    padding: 0;
    list-style: none;
    border: none;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
    backdrop-filter: none;
    width: max-content;
    max-width: none;
  }
  .site-nav > ul > li {
    display: flex;
    align-items: center;
    flex-shrink: 0;
  }
  .site-nav > ul > li > a,
  .nav-group-trigger {
    display: inline-flex;
    align-items: center;
    gap: 0.2rem;
    min-height: 60px;
    padding: 0 0.95rem;
    border-radius: 0;
    white-space: nowrap;
    text-decoration: none;
    font-size: var(--text-sm);
    font-weight: 400;
    letter-spacing: -0.16px;
    opacity: 0.8;
    background: transparent;
    box-shadow: none;
    transition: opacity 0.18s ease, background-color 0.18s ease;
    position: relative;
  }
  .site-nav > ul > li > a:hover,
  .site-nav > ul > li > a:active,
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:hover,
  .nav-group-trigger:focus-visible {
    opacity: 1;
    background: transparent;
  }
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:focus-visible {
    outline: 2px solid color-mix(in srgb, white 70%, transparent);
    outline-offset: -2px;
  }
  .site-nav > ul > li > a.active,
  .nav-group-trigger.active {
    opacity: 1;
    background: transparent;
    box-shadow: none;
  }
  .site-nav > ul > li > a.active::after,
  .nav-group-trigger.active::after {
    content: "";
    position: absolute;
    left: 0.95rem;
    right: 0.95rem;
    bottom: 10px;
    height: 2px;
    background: color-mix(in srgb, white 72%, transparent);
  }
  .nav-group {
    position: relative;
  }
  .nav-group details {
    position: relative;
  }
  .nav-group summary {
    display: inline-flex;
    align-items: center;
    gap: 0.1rem;
    list-style: none;
    cursor: pointer;
  }
  .nav-group summary::-webkit-details-marker {
    display: none;
  }
  .nav-group-chevron {
    font-size: var(--text-xl);
    opacity: 0.7;
    transition: transform 0.18s ease, opacity 0.18s ease;
  }
  .nav-group details[open] .nav-group-chevron,
  .nav-group summary:hover .nav-group-chevron {
    opacity: 1;
  }
  .nav-group details[open] .nav-group-chevron {
    transform: rotate(180deg);
  }
  .nav-submenu {
    position: absolute;
    top: calc(100% - 2px);
    left: 0;
    z-index: 120;
    display: grid;
    gap: 0.14rem;
    width: max-content;
    min-width: 16rem;
    max-width: calc(100vw - 2rem);
    margin: 0;
    padding: 0.55rem;
    list-style: none;
    border: 1px solid color-mix(in srgb, white 12%, transparent);
    border-radius: var(--radius-md);
    box-shadow: 0 12px 28px color-mix(in srgb, black 45%, transparent);
    transform-origin: top center;
    animation: nav-submenu-in 0.16s ease-out;
  }
  @keyframes nav-submenu-in {
    from {
      opacity: 0;
      transform: translateY(-4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .nav-submenu {
      animation: none;
    }
  }
  .nav-group[data-nav-group="more"] .nav-submenu {
    left: auto;
    right: 0;
  }
  .nav-submenu::before {
    content: "";
    position: absolute;
    top: -10px;
    left: 0;
    width: 100%;
    height: 12px;
    background: transparent;
  }
  .nav-submenu li {
    display: block;
  }
  .nav-submenu a {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    min-height: 3rem;
    width: auto;
    min-width: max-content;
    padding: 0.6rem 0.95rem;
    border-radius: var(--radius-sm);
    background: transparent;
    border: 1px solid transparent;
    box-sizing: border-box;
    text-decoration: none;
    opacity: 0.88;
    transition: background 0.15s ease, opacity 0.15s ease;
  }
  .nav-submenu a:hover,
  .nav-submenu a:focus-visible,
  .nav-submenu a.active {
    opacity: 1;
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .nav-submenu a.active::after {
    display: none;
  }
  .nav-submenu-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.5rem;
    flex: 0 0 1.5rem;
    font-size: var(--text-lg);
  }
  .nav-submenu-text {
    line-height: 1.25;
    font-size: var(--text-sm);
    font-weight: 400;
    letter-spacing: -0.16px;
    flex: 1 1 auto;
  }
  .nav-backdrop {
    display: none;
  }
  .nav-tool {
    display: flex;
    align-items: center;
    margin-inline-start: 0.1rem;
    padding-inline-start: 0.35rem;
    border-inline-start: 1px solid color-mix(in srgb, white 16%, transparent);
  }
  .header-utils {
    display: flex;
    align-items: center;
    gap: 0.1rem;
    flex-shrink: 0;
    margin-inline-start: 0.25rem;
  }
  .header-utils .nav-tool--bar {
    margin-inline-start: 0;
    padding-inline-start: 0;
    border-inline-start: none;
  }
  .nav-toggle {
    display: none;
  }
  .nav-toggle__close {
    display: none;
  }
  .site-nav > ul > li.mobile-priority-nav {
    display: none;
  }
  .l-header[data-nav-open] .nav-toggle__open {
    display: none;
  }
  .l-header[data-nav-open] .nav-toggle__close {
    display: inline-flex;
  }
  .nav-tool :global(.locale-trigger) {
    height: 44px;
    min-height: 44px;
    min-width: 60px;
    padding-inline: 8px;
    font-size: var(--text-sm);
    font-weight: 400;
    border: none;
    border-radius: var(--radius-sm);
  }
  .nav-tool :global(.locale-trigger:hover),
  .nav-tool :global(.locale-trigger:focus-visible) {
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .nav-tool :global(.icon-lang),
  .nav-tool :global(.icon-expand) {
    opacity: 0.85;
    font-size: var(--text-lg);
  }
  .nav-tool :global(.locale-panel) {
    border: 1px solid color-mix(in srgb, white 12%, transparent);
    box-shadow: 0 12px 28px color-mix(in srgb, black 45%, transparent);
  }
  .nav-tool :global(.locale-panel a) {
    font-size: var(--text-sm);
    font-weight: 400;
  }
  .nav-tool :global(.locale-panel a:hover) {
    background: color-mix(in srgb, white 10%, transparent);
  }
  .btn-icon {
    display: inline-flex;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: center;
    height: 44px;
    min-width: 44px;
    padding-inline: 0.55rem;
    white-space: nowrap;
    font: inherit;
    font-size: var(--text-sm);
    font-weight: 400;
    background: transparent;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: background 0.18s ease, opacity 0.18s ease;
  }
  .btn-icon:hover,
  .btn-icon:focus-visible {
    background: color-mix(in srgb, white 10%, transparent);
    border-color: transparent;
  }
  .btn-icon:focus-visible {
    outline: 2px solid color-mix(in srgb, white 70%, transparent);
    outline-offset: 1px;
  }
  .btn-icon .material-icons-sharp {
    font-size: var(--text-lg);
    color: currentColor;
    opacity: 0.95;
    flex-shrink: 0;
  }

  @media (width < 834px) {
    .l-header {
      height: auto;
      min-height: 60px;
    }
    .header-shell {
      height: auto;
      min-height: 60px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      gap: 0.35rem;
      padding-block: 0;
      padding-inline: 1rem;
      position: relative;
      z-index: 2;
    }
    .site-brand,
    .site-brand-row {
      justify-content: flex-start;
      min-width: 0;
    }
    .site-brand {
      min-height: 60px;
      align-items: center;
      overflow: hidden;
    }
    .site-brand h1 {
      min-width: 0;
      max-width: 100%;
    }
    .site-brand-link {
      max-width: 100%;
      min-width: 0;
    }
    .site-title-cobrand {
      min-width: 0;
    }
    /* Keep brand readable; hide cobrand suffix that collided with utils */
    .site-title-cobrand-x,
    .site-title-secondary {
      display: none;
    }
    .header-utils {
      grid-column: 2;
      grid-row: 1;
      position: relative;
      z-index: 3;
      gap: 0;
    }
    .nav-tool :global(.locale-trigger) {
      min-width: 44px;
      padding-inline: 6px;
    }
    .nav-toggle {
      display: inline-flex;
    }
    .site-nav {
      display: block;
      grid-column: 1 / -1;
      grid-row: 2;
      width: 100%;
      max-height: 0;
      padding-block: 0;
      border-top: 0 solid transparent;
      overflow: hidden;
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      transition:
        max-height 0.22s ease,
        opacity 0.18s ease,
        padding-block 0.18s ease,
        border-color 0.18s ease,
        visibility 0s linear 0.22s;
    }
    .l-header[data-nav-open] .site-nav {
      max-height: min(70vh, 32rem);
      padding-block: 0.35rem 0.85rem;
      border-top: 1px solid color-mix(in srgb, white 12%, transparent);
      overflow: auto;
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transition:
        max-height 0.28s ease,
        opacity 0.18s ease,
        padding-block 0.18s ease,
        border-color 0.18s ease,
        visibility 0s;
      -webkit-overflow-scrolling: touch;
    }
    .nav-backdrop {
      display: block;
      position: fixed;
      inset: 60px 0 0;
      z-index: 1;
      border: 0;
      padding: 0;
      margin: 0;
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      cursor: pointer;
      transition: opacity 0.2s ease, visibility 0s linear 0.2s;
    }
    .l-header[data-nav-open] .nav-backdrop {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transition: opacity 0.2s ease, visibility 0s;
    }
    :global(html.nav-drawer-open) {
      overflow: hidden;
    }
    .site-nav > ul {
      flex-direction: column;
      flex-wrap: nowrap;
      align-items: stretch;
      justify-content: flex-start;
      gap: 0;
      width: 100%;
      max-width: 100%;
    }
    .site-nav > ul > li {
      width: 100%;
    }
    .site-nav > ul > li.mobile-priority-nav {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 0.45rem;
      padding: 0.65rem 0 0.75rem;
      border-bottom: 1px solid color-mix(in srgb, white 12%, transparent);
    }
    .mobile-priority-nav a {
      display: grid;
      justify-items: center;
      align-content: center;
      gap: 0.25rem;
      min-height: 74px;
      padding: 0.6rem 0.35rem;
      border-radius: var(--radius-sm);
      text-align: center;
      text-decoration: none;
      background: color-mix(in srgb, white 8%, transparent);
    }
    .mobile-priority-nav a:hover,
    .mobile-priority-nav a:focus-visible {
      background: color-mix(in srgb, white 14%, transparent);
    }
    .mobile-priority-nav__icon {
      font-size: var(--text-lg);
    }
    .site-nav > ul > li > a,
    .nav-group-trigger {
      min-height: 60px;
      width: 100%;
      justify-content: flex-start;
      padding-inline: 1rem;
    }
    .site-nav > ul > li > a.active::after,
    .nav-group-trigger.active::after {
      left: 1rem;
      right: auto;
      width: 1.7rem;
      bottom: 14px;
    }
    .nav-group details {
      width: 100%;
    }
    .nav-submenu {
      position: static;
      top: auto;
      left: auto;
      right: auto;
      z-index: auto;
      margin-top: 0.15rem;
      min-width: 0;
      width: 100%;
      max-width: none;
      box-shadow: none;
      border: none;
      background: color-mix(in srgb, white 6%, transparent);
      animation: none;
    }
    .nav-group[data-nav-group="more"] .nav-submenu,
    .nav-group[data-nav-group="ai-testing"] .nav-submenu {
      left: auto;
      right: auto;
    }
    .nav-submenu::before {
      display: none;
    }
  }

  @media (width < 834px) and (prefers-reduced-motion: reduce) {
    .site-nav,
    .l-header[data-nav-open] .site-nav,
    .nav-backdrop,
    .l-header[data-nav-open] .nav-backdrop {
      transition: none;
    }
  }

  /* Zenix-inspired SaaS navigation skin. Keep behavior intact, replace the visual system. */
  .l-header {
    height: 72px;
    color: var(--color-main);
    background: color-mix(in srgb, var(--color-canvas) 78%, transparent);
    border-block-end: 1px solid rgba(148, 163, 184, 0.22);
    box-shadow: 0 16px 40px rgba(15, 23, 42, 0.06);
    backdrop-filter: blur(22px);
  }

  .header-shell {
    height: 72px;
    max-inline-size: 1440px;
    padding-inline: clamp(1rem, 3vw, 2rem);
  }

  h1 {
    font-family: var(--font-heading);
    font-size: var(--text-xl);
    font-weight: 800;
  }

  .site-brand-link,
  .site-brand-link:hover {
    color: var(--color-main);
  }

  .site-title-cobrand {
    gap: 0.34em;
  }

  .site-title-primary,
  .site-title-secondary,
  .site-brand-link > span:not(.site-title-cobrand) {
    color: var(--color-theme);
  }

  .site-title-cobrand-x {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 10%, white);
    border-color: color-mix(in srgb, var(--color-theme) 22%, transparent);
  }

  .site-nav > ul {
    gap: 0.18rem;
    padding: 0.28rem;
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.72);
    box-shadow: var(--shadow-sm);
    backdrop-filter: blur(20px);
  }

  .site-nav > ul > li > a,
  .nav-group-trigger {
    min-height: 42px;
    padding: 0 0.9rem;
    border-radius: 999px;
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text-secondary);
    opacity: 1;
  }

  .site-nav > ul > li > a:hover,
  .site-nav > ul > li > a:active,
  .site-nav > ul > li > a:focus-visible,
  .nav-group-trigger:hover,
  .nav-group-trigger:focus-visible,
  .site-nav > ul > li > a.active,
  .nav-group-trigger.active {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 9%, white);
  }

  .site-nav > ul > li > a.active::after,
  .nav-group-trigger.active::after {
    display: none;
  }

  .nav-group-chevron {
    color: currentColor;
  }

  .nav-submenu,
  .nav-tool :global(.locale-panel) {
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: var(--radius-lg);
    background: rgba(255, 255, 255, 0.94);
    box-shadow: var(--shadow-lg);
    backdrop-filter: blur(24px);
  }

  .nav-submenu a,
  .nav-submenu-text,
  .nav-tool :global(.locale-panel a) {
    color: var(--color-main);
  }

  .nav-submenu-icon {
    color: var(--color-theme);
  }

  .nav-submenu a:hover,
  .nav-submenu a:focus-visible,
  .nav-submenu a.active,
  .nav-tool :global(.locale-panel a:hover) {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 9%, white);
  }

  .header-utils {
    gap: 0.35rem;
  }

  .nav-toggle {
    display: none;
  }

  .nav-tool :global(.locale-trigger),
  .btn-icon {
    color: var(--color-text-secondary);
    background: rgba(255, 255, 255, 0.66);
    border: 1px solid rgba(148, 163, 184, 0.25);
    border-radius: 999px;
  }

  .nav-tool :global(.locale-trigger:hover),
  .nav-tool :global(.locale-trigger:focus-visible),
  .btn-icon:hover,
  .btn-icon:focus-visible {
    color: var(--color-theme);
    background: color-mix(in srgb, var(--color-theme) 9%, white);
    border-color: color-mix(in srgb, var(--color-theme) 22%, transparent);
  }

  .nav-tool :global(.icon-lang),
  .nav-tool :global(.icon-expand) {
    color: currentColor;
    opacity: 1;
  }

  @media (width < 834px) {
    .l-header,
    .header-shell {
      min-height: 64px;
      height: auto;
    }

    .site-brand {
      min-height: 64px;
    }

    .nav-toggle {
      display: inline-flex;
    }

    .l-header[data-nav-open] .site-nav {
      border-top-color: rgba(148, 163, 184, 0.22);
    }

    .site-nav > ul {
      border-radius: var(--radius-lg);
      align-items: stretch;
      background: rgba(255, 255, 255, 0.82);
    }

    .mobile-priority-nav {
      border-bottom-color: rgba(148, 163, 184, 0.22);
    }
    .mobile-priority-nav a {
      color: var(--color-text-secondary);
      background: color-mix(in srgb, var(--color-theme) 6%, white);
    }
    .mobile-priority-nav a:hover,
    .mobile-priority-nav a:focus-visible {
      color: var(--color-theme);
      background: color-mix(in srgb, var(--color-theme) 10%, white);
    }
    .mobile-priority-nav__icon {
      color: var(--color-theme);
    }

    .site-nav > ul > li > a,
    .nav-group-trigger {
      min-height: 52px;
    }

    .nav-submenu {
      background: color-mix(in srgb, var(--color-theme) 5%, white);
      border: 1px solid rgba(148, 163, 184, 0.18);
    }

    .nav-backdrop {
      inset: 64px 0 0;
      background: rgba(15, 23, 42, 0.28);
      backdrop-filter: blur(4px);
    }
  }
</style>
