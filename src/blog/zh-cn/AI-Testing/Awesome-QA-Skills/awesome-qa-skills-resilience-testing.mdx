---
title: "Resilience Testing：复核降级、隔离和残余风险"
description: "韧性测试双语指南，围绕降级行为、隔离边界、恢复能力和残余风险准备有证据边界的分析。"
date: 2026-10-25T09:00:00.000Z
draft: true
author: "nao.deng"
tags: ["AI 测试", "QA Skills", "韧性测试"]
categories: ["AI 测试", "QA Skills"]
series: ["Awesome QA Skills 实战"]
---

依赖变慢、不可用或部分失败时，系统仍能保留多少有用能力，才是韧性。正常路径冒烟测试或泛化事故描述不能代替它。

Resilience Testing Skill 分析降级行为、隔离边界、恢复能力和残余风险，并把计划中的实验与已经执行的故障测试分开。

本文关注的产物，是一个小范围、可回退，并能改变当前韧性判断的验证动作。

## 韧性测试 Skill 用来做什么

当你要复核依赖失败时服务如何降级，以及恢复或隔离证据是否就绪时，使用这个 Skill。它不会注入故障，也不会调用生产依赖。

好的结果要写清故障触发、预期降级、隔离边界、恢复能力、残余风险、负责人、停止条件和验证方式。

当准备受控韧性演练、复核依赖设计，或者调查 fallback 路径不清楚时，使用这个 Skill。

## 先回到源 Skill

完整执行规则在 [韧性测试 提示词](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing/prompts/resilience-testing.md)。[源目录](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing) 中还可能包含评测用例和补充材料。

静态计划、文件存在和 dry-run 保留原有证据状态，不能变成运行时证明、全部通过结论或发布批准。

## 用一份项目材料开始

| 材料 | 这次提供什么 | 缺失时的处理 |
| --- | --- | --- |
| 韧性目标 | 用户可见行为、服务边界和可接受降级 | 未批准目标保持 pending |
| 故障场景 | 依赖超时、部分中断、队列压力或资源耗尽 | 不要只写系统故障 |
| 隔离设计 | 舱壁、熔断、队列、fallback 和数据一致性边界 | 未文档化边界标为未知 |
| 恢复证据 | runbook、指标、告警、恢复步骤和安全环境 | 设计与执行分开 |

可以这样调用：

~~~text
请使用 resilience-testing Skill。\n\n任务：准备结账服务在税费供应商超时、支付队列延迟时的受控复核。\n输入材料：[依赖图、fallback 设计、队列策略、告警、runbook 和验证环境]\n范围：[仅 staging、不注入生产故障]\n限制：[数据脱敏和明确停止条件]\n\n覆盖降级、隔离、恢复能力和残余风险。没有依据的阈值和执行结论保持 pending。
~~~

## 让下一位同事接手

| 产物 | 要回答的问题 | 示例状态 |
| --- | --- | --- |
| 降级行为 | 用户和操作人员应该观察到什么 | 预期结果与来源 |
| 隔离边界 | 故障不能传播到哪里 | 边界与证据 |
| 恢复能力 | 正常行为如何回来 | runbook 与验证 |
| 残余风险 | 仍未知或已接受的内容 | 负责人和下一步 |

每条结论都应该能回到来源、证据状态和下一步动作。材料不足时使用 pending、blocked、未评估或 NOT_SCORED，不要用自信填空。

## 聚焦例子：复核税费超时，不要把 fallback 当成证明

设计说税费供应商超时时结账使用缓存估算，但没有说明支付捕获前如何对账过期估算。这个 Skill 应指出一致性边界，要求带停止条件的 staging 验证，在证据出现前保持恢复行为未评估。

分析可以准备隔离验证并分类设计证据，不能在授权运行产生证据前证明故障隔离、可用性或恢复能力。

## 先补基线输入，再期待更好的结果

提供故障模式、依赖和数据流图、fallback 规则、负责人、监控、runbook 版本、安全环境和停止条件。边界缺失时先请求边界，不要套用通用模式。

交接时至少保留输入版本、范围、时间窗口、证据索引、假设、人工决策边界和最小验证动作。

## 容易踩的坑

1. 只看到 fallback 返回成功，却不检查旧数据和下游一致性。
2. 列出故障模式，却不说明要隔离什么、如何判断恢复。
3. 没有定义可回退场景和升级路径，就先注入大范围故障。

## 相关 Skill

- [可靠性测试](https://inaodeng.com/zh-cn/qaskills/reliability-testing/)：把故障模式连接到可用性证据。
- [熔断器测试](https://inaodeng.com/zh-cn/qaskills/circuit-breaker-testing/)：聚焦打开、半开和关闭行为。
- [恢复测试](https://inaodeng.com/zh-cn/qaskills/recovery-testing/)：检查重大故障后的恢复路径。

## 安装与调用

~~~bash
npx skills add https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing -g
~~~

~~~text
请使用 resilience-testing Skill。
提供目标、范围、版本、证据路径、限制和决策边界。
先审计输入，保留证据状态，最后给出负责人、关闭条件和验证方式。
~~~

## 常见问题

### 这个 Skill 会直接运行混沌实验吗？

不会。它准备场景和证据计划，实际执行需要授权的操作者或测试流程。

### 写好 fallback 就够了吗？

不够。它只是设计证据，还需要隔离验证和可观察的恢复结果。



## 参考

- 韧性测试 提示词:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing/prompts/resilience-testing.md](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing/prompts/resilience-testing.md)
- 韧性测试 Skill 源文件:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/resilience-testing)
- 韧性测试 详情页:[https://inaodeng.com/zh-cn/qaskills/resilience-testing/](https://inaodeng.com/zh-cn/qaskills/resilience-testing/)
- Awesome QA Skills 项目:[https://github.com/naodeng/awesome-qa-skills](https://github.com/naodeng/awesome-qa-skills)
