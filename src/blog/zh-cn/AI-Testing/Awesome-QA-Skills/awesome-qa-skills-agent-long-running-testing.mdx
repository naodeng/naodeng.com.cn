---
title: "长运行 Agent 测试：让长任务的进度、租约和取消路径可核对"
description: "长运行 Agent 测试实战指南：检查长时间 Agent 任务中的进度、检查点、租约、取消和跨时间证据。"
date: 2026-11-03T09:00:00.000Z
draft: true
author: "nao.deng"
tags: ["AI 测试","Agent Skills","长运行 Agent 测试"]
categories: ["AI 测试","QA Skills"]
series: ["Awesome QA Skills 实战"]
---

一个长时间运行的 Agent 可以连续一个小时看起来都很健康，却在 Worker 重启时丢掉唯一有用的状态。进度条只有能对应到可验证的检查点时才有意义。

长运行 Agent 测试用于检查长时间任务是否持续推进、保存状态、续租所有权、响应取消，并留下足以解释部分结果的证据。

当团队需要围绕“进度检查点、租约续期、重启恢复、取消行为和部分结果语义”工作时，使用长运行 Agent 测试 Skill。它把模糊的测试请求变成有来源、有假设、有输出字段和下一步动作的受限分析。

[Awesome QA Skills](https://github.com/naodeng/awesome-qa-skills) 按语言和测试阶段组织 Skill。[系列总览](https://inaodeng.com/zh-cn/blog/ai-testing/introduction_of_awesome_qa_skills/) 解释通用安装方式，本文只聚焦长运行 Agent 测试。

## 长运行 Agent 测试 Skill 用来做什么


它适合这些场景：团队需要重复检查进度检查点、租约续期、重启恢复、取消行为和部分结果语义；多个证据来源互相矛盾；交接时必须说明哪些内容仍未验证。

好的结果会把进度检查点、租约续期、重启恢复、取消行为和部分结果语义接回来源和验证动作。设计清单不能直接写成通过结论。

## 先回到源 Skill

完整执行契约位于 [长运行 Agent 测试 提示词](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing/prompts/agent-long-running-testing.md)。调用 Skill 前先阅读它；提示词定义了详细流程和输出契约。[源目录](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing) 中还包含入口文件以及实际存在的配套资料。

入口文件重点约束进度检查点、租约续期、重启恢复、取消行为和部分结果语义。写结论前先阅读配套提示词，并保留这个 Skill 的决策边界。

## 用一份项目材料开始

| 材料 | 这次提供什么 | 缺失时怎么处理 |
| --- | --- | --- |
| 任务契约 | 工单数量、阶段划分、完成定义和部分结果策略 | 不要用运行时长推断完成 |
| 运行控制 | Worker 租约、检查点间隔、重启行为、取消请求和队列状态 | 没有观察到的状态变化标记为 unassessed |
| 证据 | 任务 ID、检查点记录、处理数量、重复标记和取消审计 | 没有运行记录时不要下可靠性结论 |

可以这样调用：

```text

请使用 agent-long-running-testing Skill。

任务：Agent 花两个小时处理 8 万条客服工单，期间 Worker 可能重启，用户也可能取消任务
输入材料：[需求、版本、链接、日志、报告或数据路径]
范围：[本次包含和排除的对象]
未知项：[缺失的环境、账号、数据或权限]
期望输出：[按风险排序的发现、证据状态和下一步动作]

先审计输入。分开写事实、假设和待确认问题。没有运行记录时不要声称已经执行。

```

## 长运行 Agent 测试 要交付什么

| 输出字段 | 它要回答什么 | 示例状态 |
| --- | --- | --- |
| 进度模型 | 阶段、检查点含义和数量如何映射到工作 | 可复现的进度解释 |
| 中断结果 | 重启或取消后保留了什么 | partial、cancelled、recovered 或 unknown |
| 运行动作 | 需要验证的租约、队列或存储改动 | 负责人和所需证据 |

没有运行记录、查询结果或原始产物时，不要写“已通过”。好的分析会把检查点当成持久化工作的证明，而不是心跳。它会追问同一工单是否可能被处理两次、租约是否会在阶段中途过期，以及取消后用户是否知道还有什么仍在运行。

## 在项目里跑一轮

先从一个边界清楚的小回合开始：Agent 花两个小时处理 8 万条客服工单，期间 Worker 可能重启，用户也可能取消任务。在第 37 个检查点暂停工单分类任务，重启 Worker，再把恢复后的输入集合与检查点清单比较。有效输出不是“任务恢复了”，而是恢复处理、跳过、重复处理和仍未分类的工单 ID 集合。

交接产物要同时保留输入版本、时间窗口、证据索引、负责人和下一次验证动作。Skill 可以整理不确定性，不能制造缺失的产物。

## 如何把初版做得更好

如果第一次调用只有一行目标，输出就应该保持受限。补充来源版本、受影响对象、环境、已知缺陷和决策负责人，才能从“看起来完整的清单”推进到可用评审。


## 容易踩的坑

1. 使用百分比，却没有定义哪些工作已经持久化。
2. 因为 Worker 停止就把取消任务写成成功。
3. 只跑通顺利完成的路径，忽略租约过期。

## 相关 Skill

- [Agent 故障恢复测试](https://inaodeng.com/zh-cn/qaskills/agent-failure-recovery-testing/)：覆盖长任务周围的故障与恢复决策。
- [超时测试](https://inaodeng.com/zh-cn/qaskills/timeout-testing/)：聚焦有时间边界的中断行为。
- [生产验证](https://inaodeng.com/zh-cn/qaskills/production-verification/)：检查线上进度与运行信号。



## 安装与调用

全局安装这个 Skill：
```bash

npx skills add https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing -g

```

安装后使用 `agent-long-running-testing` Skill，并附上真实项目材料。

## 常见问题

### 系统只提供一个进度数字怎么办？

把它当成待验证声明；在相信它之前要求检查点或条目级证据。



### 部分结果可以直接发布吗？

只有当契约定义了完整度、时效性和下游安全使用方式时才可以。



## 参考

- 长运行 Agent 测试 提示词:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing/prompts/agent-long-running-testing.md](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing/prompts/agent-long-running-testing.md)

- Awesome QA Skills：长运行 Agent 测试 源文件:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/agent-long-running-testing)

- Awesome QA Skills GitHub:[https://github.com/naodeng/awesome-qa-skills](https://github.com/naodeng/awesome-qa-skills)

- 长运行 Agent 测试 详情页:[https://inaodeng.com/zh-cn/qaskills/agent-long-running-testing/](https://inaodeng.com/zh-cn/qaskills/agent-long-running-testing/)
