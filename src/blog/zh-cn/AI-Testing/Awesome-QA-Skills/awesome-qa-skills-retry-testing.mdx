---
title: "Retry Testing：在重复副作用扩大前验证退避与幂等"
description: "重试测试双语指南，围绕重试触发、退避、幂等性、次数限制和重复副作用准备受控验证。"
date: 2026-11-10T09:00:00.000Z
draft: true
author: "nao.deng"
tags: ["AI 测试", "QA Skills", "重试测试"]
categories: ["AI 测试", "QA Skills"]
series: ["Awesome QA Skills 实战"]
---

当第一次请求可能已经成功时，重试会把临时故障变成业务风险。要问的不是客户端会不会再试，而是再试时会不会产生重复副作用。

Retry Testing Skill 把重试触发、退避、幂等性、次数限制和重复副作用作为一个受控行为来复核，同时区分策略描述和已经执行的重试结果。

本文关注的关键边界是客户端无法确定第一次请求是否已经生效的那段时间。

## 重试测试 Skill 用来做什么

当支付、通知、任务或 API 流程在超时和瞬态错误下可能重复请求时，使用这个 Skill。它帮助准备安全隔离的验证，不会调用生产系统。

好的复核要写清可重试条件、退避规则、幂等键、尝试上限、副作用预期、证据来源、停止条件和人工边界。

当服务要新增重试、事故暴露重复写入，或者超时路径没有明确的未知成功处理规则时，使用这个 Skill。

## 先回到源 Skill

完整执行规则在 [重试测试 提示词](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/retry-testing/prompts/retry-testing.md)。[源目录](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/retry-testing) 中还可能包含评测用例和补充材料。

静态计划、文件存在和 dry-run 保留原有证据状态，不能变成运行时证明、全部通过结论或发布批准。

## 用一份项目材料开始

| 材料 | 这次提供什么 | 缺失时的处理 |
| --- | --- | --- |
| 重试策略 | 错误类别、超时处理、退避、抖动和上限 | 未说明的行为保留 pending |
| 操作契约 | 读写语义、幂等键和副作用 | 不要只根据 HTTP 方法推断安全 |
| 故障场景 | 超时、连接重置、429、5xx 或 Worker 重启 | 选择一个受控触发 |
| 证据与环境 | 日志、请求 ID、mock 依赖、数据检查和回滚 | 不要使用生产凭证 |

可以这样调用：

~~~text
请使用 retry-testing Skill。\n\n任务：为第一次请求发生连接重置后的支付捕获客户端准备验证。\n输入材料：[重试策略、API 契约、幂等语义、请求日志、mock 供应商和数据断言]\n范围：[隔离的 staging 流程]\n限制：[不产生真实扣款、限制次数、明确清理]\n\n覆盖触发、退避、幂等、限制和重复副作用，分开计划与执行/通过结论。
~~~

## 让下一位同事接手

| 产物 | 要回答的问题 | 示例状态 |
| --- | --- | --- |
| 触发判断 | 哪些失败应该或不应该重试 | 策略与证据 |
| 时间行为 | 退避、抖动、顺序和尝试上限 | 预期时间线 |
| 副作用安全 | 如何阻止重复写入或通知 | 幂等性与数据检查 |
| 验证边界 | 什么可以安全运行、何时停止 | 负责人与回滚 |

每条结论都应该能回到来源、证据状态和下一步动作。材料不足时使用 pending、blocked、未评估或 NOT_SCORED，不要用自信填空。

## 聚焦例子：重试支付捕获，但不要扣款两次

供应商可能已经捕获支付后连接被重置。这个 Skill 应要求幂等键、有限重试，以及能区分一次和两次捕获的查询或供应商 mock。第二次 HTTP 返回成功，不等于已经证明没有重复副作用。

分析可以准备受控重试验证，不能在隔离运行产生请求和数据证据前证明幂等、扣款正确或没有重复效果。

## 先补基线输入，再期待更好的结果

提供错误分类、超时语义、API 契约、幂等行为、次数上限、退避规则、请求 ID、mock 或沙箱、数据断言和清理方式。缺一项就让对应结论保持 pending。

交接时至少保留输入版本、范围、时间窗口、证据索引、假设、人工决策边界和最小验证动作。

## 容易踩的坑

1. 不检查操作是否安全，就对所有 4xx 和 5xx 重试。
2. 只检查最终响应，不检查写入、消息或扣款次数。
3. 不明确退避和最大次数，让故障演变为请求风暴。

## 相关 Skill

- [API 幂等性测试](https://inaodeng.com/zh-cn/qaskills/api-idempotency-testing/)：按 API 契约检查重复请求。
- [超时测试](https://inaodeng.com/zh-cn/qaskills/timeout-testing/)：隔离未知完成和超时行为。
- [熔断器测试](https://inaodeng.com/zh-cn/qaskills/circuit-breaker-testing/)：检查连续失败何时应停止调用。

## 安装与调用

~~~bash
npx skills add https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/retry-testing -g
~~~

~~~text
请使用 retry-testing Skill。
提供目标、范围、版本、证据路径、限制和决策边界。
先审计输入，保留证据状态，最后给出负责人、关闭条件和验证方式。
~~~

## 常见问题

### 有幂等键就一定够了吗？

不够。幂等键、服务端行为、保留时间、响应语义和副作用证据都要符合操作契约。

### 可以对生产测试重试吗？

默认不可以。使用 mock、沙箱或隔离环境，限制次数并清理；生产验证需要明确授权。



## 参考

- [重试测试 提示词](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/retry-testing/prompts/retry-testing.md)
- [重试测试 Skill 源文件](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/retry-testing)
- [重试测试 详情页](https://inaodeng.com/zh-cn/qaskills/retry-testing/)
- [Awesome QA Skills 项目](https://github.com/naodeng/awesome-qa-skills)
