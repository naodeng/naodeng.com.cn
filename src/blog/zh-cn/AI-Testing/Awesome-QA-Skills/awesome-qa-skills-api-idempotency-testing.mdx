---
title: "API 幂等性测试 Skill：证明重复写入不会制造重复业务副作用"
description: "API 幂等性测试实战指南：测试重复请求、幂等键、并发、超时、重放窗口和结果保存。"
date: 2026-11-12T09:00:00.000Z
draft: true
author: "nao.deng"
tags: ["AI 测试", "QA Skills", "API 幂等性测试"]
categories: ["AI 测试","QA Skills"]
series: ["Awesome QA Skills 实战"]
---
重试很普通，重复退款、重复下单和重复通知不普通。幂等性测试检查的是业务副作用，而不只是第二次 HTTP 响应看起来是否熟悉。

## 为什么需要这个 Skill

API 幂等性测试用于验证相同业务意图的重复或并发请求只产生一次安全副作用，并返回定义清楚的结果。

## Skill 用来做什么

当团队需要围绕“幂等键语义、重放、并发、超时恢复、结果持久化和冲突”工作时，使用 API 幂等性测试 Skill。

[Awesome QA Skills](https://github.com/naodeng/awesome-qa-skills) 按语言和测试阶段组织 Skill。[系列总览](https://inaodeng.com/zh-cn/blog/ai-testing/introduction_of_awesome_qa_skills/) 解释通用安装方式，本文只聚焦 API 幂等性测试。

它适合这些场景：团队需要重复检查幂等键语义、重放、并发、超时恢复、结果持久化和冲突；多个证据来源互相矛盾；交接时必须说明哪些内容仍未验证。

结果至少要把幂等键语义、重放、并发、超时恢复、结果持久化和冲突接回来源和验证动作。设计清单不能直接写成通过结论。

## 它不做什么

API 幂等性测试 Skill 可以组织重放结果的材料、证据和下一步动作，但它不能替代：

- 业务或领域负责人对规则、范围和风险的确认。
- 真实环境、账号、数据、日志或运行结果；静态分析不会自动升级为运行证据。
- 发布、合规、生产操作和残余风险接受等授权决定。
- 在输入相互冲突时替团队选择一个未经确认的事实。

## 它主要检查哪些内容

这个 Skill 的价值不在于把关键词再列一遍，而在于把每个关注点接到可观察的输入、判断和收口动作。先用源 Skill 已定义的输出字段整理一张小矩阵：

| 检查关注点 | 开始前要问 | 结果应留下什么 |
| --- | --- | --- |
| 重放结果 | 同一个键是否返回原始结果 | 响应与持久化状态 |
| 副作用数量 | 创建了多少订单、扣款、事件或通知 | 一次、零次或重复 |
| 冲突发现 | 参数变化、键过期或并发归属冲突 | 策略与修复动作 |

如果某一行只有经验判断、没有来源或验证方法，就把它留在待确认项里，不要提前写成通过。

## 开始之前先审计输入

围绕“重放结果”开始分析时，先把输入分成六种状态。缺口不会自动变成失败，但也不能被隐藏在结论里。

| 状态 | 含义 | 本 Skill 的处理方式 |
| --- | --- | --- |
| known | 材料直接证明的事实 | 标出来源、版本和时间，允许进入判断 |
| missing | 本轮需要但尚未提供的材料 | 列出最小补证动作，结论保持受限 |
| conflicting | 来源之间互相矛盾 | 并列来源和冲突点，交给负责人裁决 |
| stale | 材料存在但版本或时间已经过期 | 标出新鲜度，不把旧结论当当前事实 |
| out_of_scope | 有关但不在本轮范围内 | 明确排除，避免分析悄悄扩大 |
| assumptions | 为继续分析而暂时采用的假设 | 写出验证方式和失效条件 |

至少把输入版本、范围、环境、证据位置和负责人放在一起。没有运行记录时，只能交付分析、设计或验证计划，不能写执行通过。

## 从问题到结构化 Finding

先把来源、范围、证据状态、分析判断、责任角色、动作、关闭条件和验证方式连起来，再写结论。下面的案例保留这个 Skill 的专属编号和领域语境。

### 分层写法

围绕重放结果，不要把四种语气揉成一句“建议通过”：

| 层次 | 写法 | 本文应用 |
| --- | --- | --- |
| Fact | 材料直接显示了什么 | 引用来源中的重点、版本、输入或运行记录 |
| Evidence-backed Inference | 多个事实共同支持的判断 | 说明推断链，并保留不确定性 |
| Recommendation | 下一步最小动作是什么 | 指定补证、复核、执行或回归路径 |
| Human Decision | 谁需要决定什么 | 由负责人确认范围、风险接受、资源和发布含义 |

## 一个完整案例

本节按输入（Input）、分析（Analysis）、Finding、决策（Decision）和验证（Validation）展开；材料不足时保留 missing、conflicting 或 assumptions，不把它们改写成通过。

### 输入（Input）

| 材料 | 这次提供什么 | 缺失时怎么处理 |
| --- | --- | --- |
| 操作契约 | 写入副作用、幂等键格式、重放窗口和冲突策略 | 不要从缓存设置推断重放窗口 |
| 请求变体 | 同键同参数、同键改参数、无键和并发请求 | 记录完整请求体与请求头 |
| 业务证据 | 订单 ID、支付尝试、事件记录和响应体 | 没有状态证据时，响应一致还不够 |

可以这样调用：

```text

请使用 api-idempotency-testing Skill。

任务：测试结算 POST 在客户端超时但服务端已经创建订单后的行为
输入材料：[需求、版本、链接、日志、报告或数据路径]
范围：[本次包含和排除的对象]
未知项：[缺失的环境、账号、数据或权限]
期望输出：[按风险排序的发现、证据状态和下一步动作]

先审计输入。分开写事实、假设和待确认问题。没有运行记录时不要声称已经执行。

```

### 分析（Analysis）

先从一个边界清楚的小回合开始：测试结算 POST 在客户端超时但服务端已经创建订单后的行为。使用 `cart-1842` 提交结算，让客户端超时后分别串行和并发重放两次。确认最终只有一个订单、一次支付尝试和一个确认事件，响应指向原订单。同一个键换一个购物车时，应返回冲突。

交接产物要同时保留输入版本、时间窗口、证据索引、负责人和下一次验证动作。Skill 可以整理不确定性，不能制造缺失的产物。

### Finding

#### 示例发现：把一次问题写成可交接动作

下面的字段示例只展示记录方式，不代表真实环境已经执行。

假设当前材料无法证明“重放结果”已经满足要求，发现可以这样写。它既不替团队补写规则，也不把缺失证据伪装成失败。

| 字段 | 示例写法 |
| --- | --- |
| 来源与范围 | 记录本轮使用的需求、版本、环境和重放结果的具体对象 |
| 发现 | 重放结果的判断条件或结果仍缺少可追溯依据 |
| 证据状态 | missing / assumptions；如果来源相互矛盾则改为 conflicting |
| 影响与优先级 | 说明会影响哪个用户、链路或交付决定，不凭感觉扩大等级 |
| Owner 与 Human 决策 | 由业务、开发、安全或测试负责人确认规则和风险取舍 |
| 动作与关闭条件 | 补齐最小证据；关闭条件是来源、判断和责任人都能复核 |
| 验证 | 指定一次可重复的检查、查询或运行，并保存原始产物 |

这类记录的重点是让下一位同事能从发现回到来源，再执行一项能改变结论的动作。

### 决策（Decision）

责任角色和待决策问题应由对应负责人确认；Skill 不替他们做风险取舍。

### 验证（Validation）

关闭前按 Finding 中的验证方式执行，并保留原始产物；没有执行记录时，状态仍然保持未验证。

## Finding 怎么进入下一阶段

### 交接产物

| 输出字段 | 它要回答什么 | 示例状态 |
| --- | --- | --- |
| 重放结果 | 同一个键是否返回原始结果 | 响应与持久化状态 |
| 副作用数量 | 创建了多少订单、扣款、事件或通知 | 一次、零次或重复 |
| 冲突发现 | 参数变化、键过期或并发归属冲突 | 策略与修复动作 |

没有运行记录、查询结果或原始产物时，不要写“已通过”。好的幂等性测试会把请求追到存储和下游事件，并测试同一个键配上不同参数的情况。悄悄接受这种请求通常比明确拒绝更危险。

### 下一步路由

至少交付来源、证据状态、负责人、关闭条件和验证动作；下一阶段的结论仍受证据状态约束。

## 怎样准备更好的输入

如果第一次调用只有一行目标，输出就应该保持受限。补充来源版本、受影响对象、环境、已知缺陷和决策负责人，才能从“看起来完整的清单”推进到可用评审。

## 和其他 Skill 怎么配合

- [API 契约测试](https://inaodeng.com/zh-cn/qaskills/api-contract-testing/)：固定请求与响应契约。
- [重试测试](https://inaodeng.com/zh-cn/qaskills/retry-testing/)：探索重试触发与边界。
- [API 错误契约测试](https://inaodeng.com/zh-cn/qaskills/api-error-contract-testing/)：检查冲突与处理中响应。

## 容易踩的坑

1. 只检查响应重复，不检查副作用重复。
2. 只测试串行重试。
3. 让改变参数的请求悄悄继承旧幂等键。

## 安装与调用

全局安装这个 Skill：
```bash

npx skills add https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing -g

```

安装后使用 `api-idempotency-testing` Skill，并附上真实项目材料。

## FAQ

### 第一次响应丢失怎么办？

重放应恢复原始业务结果或明确的处理中状态，不能再次制造副作用。



### 按时间过期的幂等键就够了吗？

只有契约定义过期后的行为，且证据证明业务风险可接受时才够。

## 参考

### 源 Skill 与执行契约

完整执行契约位于 [API 幂等性测试 提示词](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing/prompts/api-idempotency-testing.md)。调用 Skill 前先阅读它；提示词定义了详细流程和输出契约。[源目录](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing) 中还包含入口文件以及实际存在的配套资料。

入口文件重点约束幂等键语义、重放、并发、超时恢复、结果持久化和冲突。写结论前先阅读配套提示词，并保留这个 Skill 的决策边界。

### 参考链接

- API 幂等性测试 提示词:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing/prompts/api-idempotency-testing.md](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing/prompts/api-idempotency-testing.md)

- Awesome QA Skills：API 幂等性测试 源文件:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/zh/testing-types/api-idempotency-testing)

- Awesome QA Skills GitHub:[https://github.com/naodeng/awesome-qa-skills](https://github.com/naodeng/awesome-qa-skills)

- API 幂等性测试 详情页:[https://inaodeng.com/zh-cn/qaskills/api-idempotency-testing/](https://inaodeng.com/zh-cn/qaskills/api-idempotency-testing/)
