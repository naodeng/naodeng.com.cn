---
title: "API Error Contract Testing Skill: Make failure responses predictable for every consumer"
description: "API Error Contract Testing field guide: validate error envelopes, codes, retryability, field paths, security, and consumer behavior."
date: 2026-11-11T09:00:00.000Z
draft: true
author: "nao.deng"
tags: ["AI Testing", "QA Skills", "API Error Contract Testing"]
categories: ["AI Testing","QA Skills"]
series: ["Awesome QA Skills Field Guides"]
---
The successful response usually gets documented first. Then a consumer receives three different error shapes for validation, authorization, and dependency failure—and somebody calls that an API contract.

## Why this Skill is needed

API Error Contract Testing checks whether failures have stable structure, useful classification, safe detail, and enough information for clients to choose the right next action.

## What the Skill does

The API Error Contract Testing Skill is useful when a team needs a structured review of error envelope, code taxonomy, field paths, retryability, redaction, and client handling.

[Awesome QA Skills](https://github.com/naodeng/awesome-qa-skills) organizes Skills by language and testing stage. The [series overview](https://inaodeng.com/en/blog/ai-testing/introduction_of_awesome_qa_skills/) explains the shared installation model; this guide stays with API Error Contract Testing.

Use it when the team needs a repeatable review of error envelope, code taxonomy, field paths, retryability, redaction, and client handling—especially when several evidence sources disagree or a handoff must explain what remains unverified.

A useful result keeps error envelope, code taxonomy, field paths, retryability, redaction, and client handling connected to a source and a verification action. It does not turn a design checklist into a passing result.

## What it does not do

The API Error Contract Testing Skill can organize material, evidence, and next actions for Error finding. It does not replace:

- Confirmation of rules, scope, and risk by the accountable domain owner.
- A real environment, account, dataset, log, or run record; static analysis does not become runtime evidence by itself.
- Authorization for release, compliance, production actions, or residual-risk acceptance.
- A Human decision when supplied sources conflict.

## What it checks

The value of this Skill is not another keyword list. It connects each focus area to an observable input, a judgment, and a way to close the loop. Start with a small matrix based on the source Skill's output contract:

| Focus | Question before analysis | Handoff output |
| --- | --- | --- |
| Error finding | Shape, classification, field location, or security problem | Contract source and impact |
| Consumer action | Retry, correct input, re-authenticate, wait, or stop | Expected decision |
| Compatibility note | Change that breaks or weakens a client assumption | Migration or version action |

If a row has only a conventional expectation and no source or validation method, keep it open instead of turning it into a pass.

## Audit inputs before you start

Before analyzing Error finding, classify the input into six evidence states. A gap is not automatically a failure, but it must not disappear inside the conclusion.

| State | Meaning | How this Skill should handle it |
| --- | --- | --- |
| known | Directly supported by the supplied material | Keep the source, version, and time with the judgment |
| missing | Needed for this pass but not supplied | Name the smallest evidence action and limit the conclusion |
| conflicting | Sources disagree | Show both sources and route the conflict to an owner |
| stale | Present but outside the relevant version or time window | Mark freshness; old evidence is not current proof |
| out_of_scope | Related but excluded from this pass | Keep the boundary explicit |
| assumptions | Temporarily adopted to continue analysis | State how and when the assumption will be checked |

Keep the input version, scope, environment, evidence locations, and accountable owner together. Without a run record, deliver analysis, design, or a validation plan—not an execution pass.

## From problem to structured Finding

Connect the source, scope, evidence state, analysis, owner, action, close condition, and validation before writing the conclusion. The case below keeps this Skill’s identifier and domain context.

### Keep the decision layers separate

For Error finding, do not compress four different kinds of language into “recommended to pass”:

| Layer | How to write it | Application here |
| --- | --- | --- |
| Fact | What the supplied material directly shows | Cite the source, version, input, or run record for the focus |
| Evidence-backed Inference | What several facts support together | Show the inference chain and retain uncertainty |
| Recommendation | The smallest next action | Name the evidence, review, execution, or regression path |
| Human Decision | What an accountable person must decide | Leave scope, risk acceptance, resources, and release meaning to the owner |

## A complete case

This case follows Input, Analysis, Finding, Decision, and Validation. When material is incomplete, keep missing, conflicting, or assumptions visible instead of turning them into a pass.

### Input

| Material | What to provide | What to do when it is missing |
| --- | --- | --- |
| Error contract | Envelope, code, message, field path, request ID, and status mapping | Mark optional fields that clients currently rely on |
| Failure matrix | Validation, authentication, authorization, conflict, rate limit, and dependency failure | Do not collapse different actions into one generic case |
| Consumer behavior | Retry, display, logging, fallback, and support lookup | Trace each action to an error field |

Use a request like this:

```text

Use the api-error-contract-testing Skill.

Task: validate errors from a payment API used by a checkout service and a mobile client
Inputs: [requirements, versions, links, logs, reports, or data paths]
Scope: [included and excluded objects]
Unknowns: [missing environment, accounts, data, or permissions]
Expected output: [risk-ordered findings, evidence status, and next actions]

Audit the inputs first. Separate facts, assumptions, and open questions. Do not claim execution without a run record.

```

### Analysis

Start with a bounded pass—validate errors from a payment API used by a checkout service and a mobile client. Send an invalid card, an expired authorization token, and a provider timeout through the same checkout endpoint. Assert the envelope and then assert the client action: fix input, re-authenticate, or retry after a delay. Also check that provider details are redacted.

The useful handoff keeps the input version, time window, evidence index, owner, and next validation action together. The Skill can organize uncertainty; it cannot manufacture the missing artifact.

### Finding

#### Example finding: turn one problem into a handoff

The field example below shows the recording pattern; it is not an execution result.

If the supplied material cannot prove that Error finding meets its contract, write the finding like this. It does not invent the missing rule or turn missing evidence into a failure.

| Field | Example wording |
| --- | --- |
| Source and scope | Record the requirement, version, environment, and the concrete object for Error finding |
| Finding | The condition or result for Error finding is not yet traceable to evidence |
| Evidence state | missing / assumptions; use conflicting when sources disagree |
| Impact and priority | Name the affected user, journey, or delivery decision without inflating severity |
| Owner and Human decision | Ask the product, engineering, security, or test owner to confirm the rule and trade-off |
| Action and close condition | Add the smallest missing evidence; close only when source, judgment, and owner can be reviewed |
| Validation | Name one repeatable check, query, or run and retain the raw artifact |

The point is to let the next person walk from the finding back to the source and run an action that can change the decision.

### Decision

The accountable owner confirms the decision question and risk trade-off; the Skill does not make that choice.

### Validation

Before closing the finding, run the stated validation and retain the raw artifact. Without an execution record, the status remains unverified.

## How a Finding enters the next stage

### Handoff output

| Output field | Why it exists | Example status |
| --- | --- | --- |
| Error finding | Shape, classification, field location, or security problem | Contract source and impact |
| Consumer action | Retry, correct input, re-authenticate, wait, or stop | Expected decision |
| Compatibility note | Change that breaks or weakens a client assumption | Migration or version action |

Do not write “passed” without a run record, query result, or source artifact. A useful test starts from the client decision, not the server implementation. If a mobile client cannot distinguish a temporary provider outage from an invalid card, the error contract has failed even when every response is valid JSON.

### Next-stage route

At minimum, hand off the source, evidence state, owner, close condition, and validation action; the next-stage conclusion remains bounded by the evidence state.

## How to prepare better input

If the first request contains only a one-line goal, the output should stay limited. Add the source version, affected objects, environment, known defects, and decision owner to move from a plausible checklist to a useful review.

## Working with other Skills

- [API Contract Testing](https://inaodeng.com/en/qaskills/api-contract-testing/):covers the wider request and response contract.
- [API Negative Testing](https://inaodeng.com/en/qaskills/api-negative-testing/):generates invalid and boundary inputs.
- [API Security Testing](https://inaodeng.com/en/qaskills/api-security-testing/):checks leakage and authorization failure handling.

## Common traps

1. Asserting only HTTP status.
2. Letting human-readable messages become machine contracts.
3. Returning provider or database details that expose internal state.

## Install and invoke

Install the individual Skill. The series overview carries the longer installation explanation.

```bash

npx skills add https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing -g

```

After installation, invoke it with the `api-error-contract-testing` Skill and attach the real project material.

## FAQ

### Should every error have a unique code?

Not necessarily, but each code must support a distinct consumer action or a deliberate shared policy.



### Can snapshots prove the contract?

They can catch shape changes, but they need semantic assertions for retryability, privacy, and client behavior.



The example above is a design and review pattern, not an execution result. Keep static analysis, runtime evidence, human approval, and release acceptance separate.

## References

### Source Skill and execution contract

The complete execution contract lives in the [API Error Contract Testing prompt](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing/prompts/api-error-contract-testing.md). Read it before invoking the Skill; the prompt defines the detailed workflow and output contract. The [source directory](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing) also contains the entry point and supporting assets where they exist.

The entry point centers error envelope, code taxonomy, field paths, retryability, redaction, and client handling. Read the supporting prompt before writing a conclusion, and keep the Skill's decision boundary visible.

### Reference links

- API Error Contract Testing:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing/prompts/api-error-contract-testing.md](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing/prompts/api-error-contract-testing.md)

- Awesome QA Skills: API Error Contract Testing source:[https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing](https://github.com/naodeng/awesome-qa-skills/tree/main/skills/en/testing-types/api-error-contract-testing)

- Awesome QA Skills GitHub:[https://github.com/naodeng/awesome-qa-skills](https://github.com/naodeng/awesome-qa-skills)

- API Error Contract Testing details:[https://inaodeng.com/en/qaskills/api-error-contract-testing/](https://inaodeng.com/en/qaskills/api-error-contract-testing/)
