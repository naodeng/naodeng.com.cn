# docs

长期保存的项目文档目录。

- `requirements/`: 需求与 PRD
- `design/`: 设计方案与决策记录
- `tasks/`: 执行任务清单与里程碑
- `architecture/`: 模板、架构约定与技术基线
- `standards/`: 规范文档（如内容/格式标准）
- `temporary/`: 临时探索与脚本输出

约定：
- 新的长期文档应放入 `docs/`。
- 文档内引用路径应使用当前目录结构，避免历史路径残留。
